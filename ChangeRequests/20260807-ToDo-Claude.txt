===============================================================================================================================================================
= HVF Web Application
===============================================================================================================================================================

===============================================================================================================================================================
= Setting priorities
===============================================================================================================================================================

    These are guidelines for tasks that do not have set priorities - these are NOT actions

    -- P-01 for BUGS
    -- P-02 for queries
    -- P-06 for move, remove, change tasks
    -- P-10 for new functionality
    -- P-20 for documentation
    -- P-25 for Mobile device tasks
    -- Completed rows must include Claude near the end of the completion note, immediately before the final status marker (changed from "OpenAI" 2026-08-07 per user — the prior wording misattributed the work)

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - BUGS
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-03 "VolumeScore — breakout confirmation (0–12)" is flashing … and content below -- Root cause: the 2026-08-05 "standardize loading states" commit added the pulsing `sqh-loading` CSS class to the placeholder div, but the fetch success-handler never removed it after painting real content, so the box (and everything below it) kept pulsing forever. Added `classList.remove("sqh-loading")` to the handler, matching the already-correct pattern used elsewhere (e.g. paintXVisuals). 32 tests passed. Claude. [Completed]
    * P-03 "Tab visibility" is flashing … and content below -- Same root cause and fix as the VolumeScore card, applied to `renderTabVis()`'s `tabvis-list` div. 32 tests passed. Claude. [Completed]
    * P-03 "Squeeze Rules 1–5 — justification no rule detail" is flashing -- Same root cause and fix, applied to the rules handler's `rulesbox` div. While investigating, found the identical defect (class added 2026-08-05/06, never cleared) on two unreported cards — Fundamentals (`fundbox`) and Broker analysis (`brokerchg`) — and fixed those too since the cause and fix are identical. 32 tests passed. Claude. [Completed]
    * P-03 Full sitewide audit (user 2026-08-07, "look at all text that flashes... it is only when data is loading that it is used") -- Claude 2026-08-07: checked every element that can carry the pulsing `sqh-loading` class (39 occurrences) against every code path that could complete its load. Found and fixed 7 more with the identical missing-`classList.remove` defect, all previously unreported: On X panel (`xlinks`), X thread (`tweetbox`), Configuration → Execution sources (`cfg-exec`) and Credentials (`cfg-creds`), Configuration (Admin) → Squeeze markets picker (`xhvf-markets`), the Best Settings summary cards wrapper (`ordp-summary` — the outer div itself carried the class in static markup), and the User Management → IG account audit reveal (which used to overwrite its whole className, dropping both the loading AND its normal "muted" style — moved to classList add/remove so the base style survives too). Verified every remaining instance is either already correct (e.g. `cfg-defaults`, `best-settings-report`, `paintXVisuals`) or self-resolving (the whole element carrying the class gets replaced wholesale by real content, e.g. `sqh-rows`, `jobs-rows`, `mk-count`) — including the "refresh" button's pulse, which is intentionally tied to a real in-progress snapshot rebuild, not a bug. 38 tests passed. Claude. [Completed]
    * P-01 (user 2026-08-11) "I would like to be sure that my trading filter settings are being used to choose instruments to order — including all settings e.g. ATR, VWAP, R:R etc"
      -- Claude 2026-08-11: traced every caller of ig_shim.place_hvf_order_from_sig (the single guarded
      order-placement chokepoint) before changing anything. Found: Configuration → My trading limits
      (min R:R, min Quality, min VolumeScore, min RVOL, require-above-VWAP, require-ATR-expanding,
      instrument value band) only ever gated hvf_web/server.py's _limit_block(), called from exactly TWO
      routes — /api/preorder-pin and /api/place-order — i.e. a user's own MANUAL web actions. The
      automated engine (hvf_web/order_bridge.py's 2h sweep across the full universe, and the session-based
      cron jobs AUS/UK/US Open + *_HVF_WATCH + *_MONITOR via run_session.py/intraday_signals.py) never
      imports hvf_web/server.py and so never saw these floors — it only enforced a single SHARED
      admin-level quality floor (bridge_min_quality) plus admin Trade Filters (direction/market/location).
      An instrument failing your personal R:R or ATR floor could be placed as a real IG working order by
      the bridge/session jobs even though you personally couldn't pin or manually place it. User decision:
      the automated engine should honour the account owner's personal floors too. Fix: extracted the
      floor-check logic into a new shared module, trading_limits.py (limit_defaults/user_limits/
      check_limits), used by BOTH hvf_web/server.py (_limit_block now delegates to it, preserving its
      existing UI wording/behaviour exactly) and ig_shim.py (new). Wired trading_limits.check_limits into
      place_hvf_order_from_sig immediately after the existing admin quality-floor check, keyed on
      profile["name"] (the account this run is trading as). Handles TWO different field-naming schemes
      across the codebase so every caller is actually covered: hvf_web/build_snapshot.py's boolean
      above_vwap/atr_expanding (order_bridge.py, manual place-order — extended order_bridge.py's and
      hvf_web/server.py's _sig_from_snapshot's sig dicts to carry these, previously dropped) AND
      signals.py's vwap_position ("ABOVE"/"BELOW") / pa_atr_expanding (the session monitors/opens) — both
      translated to the same above_vwap/atr_expanding booleans before the check. R:R and Quality are
      enforced on every caller (already universally present in sig dicts). Fails OPEN on any error or
      missing field (matches every other Config gate in this function) so a bug in the new check can never
      itself block trading. KNOWN GAP, documented rather than silently left implicit: RVOL, VolumeScore
      and instrument-value(mcap) are NOT yet enforced on the automated path — they require a bulk
      price-history fetch (RVOL/VolumeScore) or a separate Supabase join (mcap) that only happens at the
      hvf_web/server.py API-request layer today, not inside the lightweight snapshot/sig objects the engine
      builds; wiring those in is a larger follow-up (see trading_limits.py's module docstring). They ARE
      still enforced for manual actions, which already had this data via _record(). 14 new tests
      (test_trading_limits.py) including one that asserts a blocked setup never reaches get_epic (i.e. the
      IG-facing part of the function) — not just "logs a block and places anyway" — plus full existing
      suites (test_server_auth.py, test_ig_transactions.py, test_hvf_method.py) still pass. Claude.
      [Completed — RVOL/VolumeScore/instrument-value on the automated path remains a follow-up, see gap note above]

    * P-01 follow-up (user 2026-08-11) "Check all instruments have current rvol, volumescore, above VWAP and
      above ATR metrics - plus a sector - the only area without a sector would be indices as these are multi
      sector"
      -- Claude 2026-08-11: read the live snapshot.json (1421 records) and queried Supabase directly rather
      than assume. Findings: (1) above_vwap/atr_expanding are None on 100% (269/269) of has_signal records —
      root cause: price_action.get_hvf_signal_mtf(), the function that actually produces build_snapshot.py's
      signal rows, never computes VWAP position at all and never merges in atr_expanding from the separate
      analyse_price_action() helper (which does compute it, just isn't called here). This has been true since
      those fields were added — never a live-data staleness issue. The Scanner tab itself was unaffected
      (api_records() already recomputes both fresh from volume_score.py's component breakdown, which is how
      the P-09 VWAP/ATR ticks work correctly) — but it meant the SAME-DAY P-01 fix above was reading this
      dead field in order_bridge.py's _candidates() and hvf_web/server.py's _sig_from_snapshot(), so "Require
      above VWAP"/"Require ATR expanding" silently never blocked anything on the automated 2h-bridge or
      manual place-order paths. Fixed: added hvf_web/server.py's _live_vwap_atr(), reusing the exact
      volume_score.py source api_records() already trusts, and pointed both callers at it instead of the
      static field. 7 new tests (test_live_vwap_atr.py), full suite (111 tests) still passes. (2) RVOL/
      VolumeScore: by design only computed for TRIGGERED rows (147 of 1421); verified via direct Supabase
      query that price_history has healthy, current coverage (131-143 daily bars, none older than 3 days) for
      all 147 — the underlying data has no gap. Could not execute the actual Python computation from the
      sandbox (no direct Postgres connectivity there, unlike the live web server), but there is nothing to
      fix — no code or data gap found. (3) Sector: 332 of 1421 records show no sector. Of those, 115 are
      correctly resolved as sector-less in sector_cache.json (investment trusts — Foreign & Colonial IT,
      Scottish Mortgage IT, etc. — Yahoo Finance has no GICS sector for these; working as designed). The
      remaining 217 were never resolved at all: ~131 are ordinary equities (incl. Tesco, Unilever, Vodafone,
      Weir Group on FTSE 100; Tesla, Texas Instruments on NASDAQ 100; plus FTSE 250/S&P 500/DAX/ASX/Euronext/
      NSE/SZSE names) that should have a real sector and don't purely because sector_cache.backfill() has
      never reached them (likely a Yahoo rate-limit cutoff partway through an earlier run) — a genuine gap,
      but NOT a code bug: sector_cache.py's existing `python sector_cache.py` entry point already does
      exactly the right thing (resolves anything not yet cached, checkpoints every 50). Could not run it from
      the sandbox (no network route to Yahoo Finance here) — needs running locally: `python sector_cache.py`.
      The other ~86 of the 217 (Government Bonds, Indices, FX, Commodities, Crypto) legitimately have no
      GICS sector, matching the spirit of "the only exception is indices" — just a slightly wider category
      than literally indices (bonds/FX/commodities/crypto are the same kind of non-equity exception). Claude.
      [Completed — sector backfill for the ~131 real gaps needs `python sector_cache.py` run locally, see note above]

    * P-01 follow-up 2 (user 2026-08-11) "The scanner report MUST also match the user trading filter
      settings"
      -- Claude 2026-08-11: verified (not assumed) before concluding anything. Found the Scanner Report tab
      and its daily email use a separate, smaller "Scanner Report filter defaults" (Quality/R:R/RVOL/PE/
      expiry ranges — no VWAP/ATR fields at all), confirmed genuinely distinct from "My Trading Filters" at
      three places in the code (hvf_web/index.html's own UI copy at the Preferences card, run_
      scanner_report_email.py's header/comments, and applyConfigFromReport()'s explicit limits/filters
      separation) — a deliberate original design, not a bug. User chose "Hard filter (Recommended)": setups
      failing My Trading Filters must be hidden from the Scanner Report table and excluded from its email,
      not just flagged. Scanner Report TABLE done: added a MY_LIMITS hard-filter block to index.html's
      pass() — the table's single filter chokepoint (confirmed via grep it's used ONLY by the Scanner table/
      charts, not My Pre-orders or other tabs) — mirroring renderPreorders()'s existing floor logic exactly
      so the two views can't disagree. User then reported (2026-08-11) still seeing ATR-failing setups after
      saving "Require ATR expanding" and refreshing. Investigated rather than assumed a stale cache: traced
      it to a REAL second gap the user's own question surfaced ("for any one day you should be able to
      calculate these values, shouldn't you?" — correct). above_vwap/atr_expanding were being read from
      volume_score.py's per-ticker "components", which — like RVOL/VolumeScore — are only ever computed for
      TRIGGERED setups on their break bar; READY/DEVELOPING setups (~45% of a day's has_signal rows) got
      None regardless of their true state, so the new hard filter (which fails open on unknown, matching
      every other gate in this codebase) silently let them through unproven. Fixed properly rather than
      just hiding the unproven rows: volume_score._above_vwap()/_atr_expanding() take ANY bar index, not
      specifically a trigger bar, so hvf_web/server.py's _live_vwap_atr() now fetches fresh bars for EVERY
      has_signal ticker (any status) and reads the LATEST bar directly — a live "is it above VWAP / is ATR
      expanding right now" read, arguably more correct for a live trading decision than a stale trigger-day
      snapshot anyway. Wired into api_records() too, so the Scanner's own VWAP/ATR ticks are now accurate
      for READY/DEVELOPING rows, not just TRIGGERED. 7 tests rewritten/added, full suite (111) passes.
      Email done too: run_scanner_report_email.py's user_rows() now ALSO runs each row through
      trading_limits.check_limits(username, ...) — the same shared rulebook as the Scanner tab and the
      automated order-placement gate — on top of the existing "Scanner Report filter defaults", and
      build_rows() sources above_vwap/atr_expanding from the same fixed _live_vwap_atr() so a READY/
      DEVELOPING row's VWAP/ATR state is real there too, not just on the tab. Footer text and MAX_ROWS
      "no setups" copy updated so recipients know both filter layers apply. 4 tests added/updated
      (test_scanner_report_email.py), full suite (114 tests) passes. Claude. [Completed]

    * P-01 (user 2026-08-11) "Despite setting trading filters with ATR and VWAP with a tick we still have"
      [Scanner rows showing ATR ✗ / no VWAP requirement enforced, e.g. MARUTI.BO, HUB24] --
      Claude 2026-08-11: root-caused rather than guessed, by reading render() and pass() (the Scanner's
      single filter chokepoint, which already correctly hard-filters on MY_LIMITS.require_atr_expanding/
      require_above_vwap since the same-day P-01 fix above). Found THREE separate places where a real
      code path updates MY_LIMITS but never re-renders the Scanner table to match, so newly-filtered rows
      don't disappear: (1) saveLimits() — Configuration -> My Trading Filters -> Save — re-rendered My
      Pre-orders + Performance but not the Scanner table; (2) applyConfigFromReport() — the Best Settings
      "Apply this configuration" button — same gap; (3) the MOST LIKELY actual cause of what the user saw:
      the initial page-load boot sequence (the Promise.all fetching /api/records + /api/config together)
      never read cfg.limits into MY_LIMITS at all — only cfg.filters/cfg.trade/cfg.markets_off — so
      MY_LIMITS stayed {} for the ENTIRE session unless the user happened to separately visit the
      Configuration tab (the only other place that ever read cfg.limits, via renderConfig()). A floor
      saved in an earlier session had NO effect on a fresh page load, with no Save click involved at all.
      All three fixed: (1) and (2) now also call render() after updating MY_LIMITS; (3) now loads
      cfg.limits into MY_LIMITS before the boot sequence's first render() call, mirroring how
      USER_FILTERS/TRADE_HIDE/MARKETS_DISABLED/MARKETS_OFF are already loaded from the same response.
      User then asked directly "did you not test this?" — correct challenge: the first pass was
      code-reading only, no executable test, which is a real and repeated gap across every client-JS fix
      made today (none of today's other JS changes had test coverage either — this repo has no
      execution harness for hvf_web/index.html's inline JS, only a syntax-check test). Per user directive
      "resolve poor testing going forward, actioned now": added two new tests to test_performance.py —
      test_scanner_rerenders_after_every_my_limits_mutation() (extracts saveLimits()/
      applyConfigFromReport()/the boot handler's source via a small function-boundary helper and asserts
      each actually calls render(), and that MY_LIMITS is assigned before render() in the boot sequence —
      so this exact regression, or a new function added with the same gap, fails the suite) and
      test_pass_still_hard_filters_on_my_limits_atr_and_vwap() (confirms pass() itself still contains the
      checks the other test is guarding, so the guard can't silently become a no-op). These are static
      structural checks against the real index.html source — the same house convention already used by
      test_performance_best_settings_is_a_dedicated_wallet_constrained_tab — not a full behavioural/DOM
      test (no jsdom harness exists in this repo), but real, executable coverage where there was none.
      116 tests pass (114 previous + 2 new). Claude. [Completed]

    * P-02 (user 2026-08-11) "It's worth adding a note that it is not a history of squeezes BUT a list of
      actionable squeezes" -- Claude 2026-08-11: added to two places. (1) The live Scanner page itself
      (hvf_web/index.html) — a muted note under the existing lede: "This is a live list of what's
      actionable right now, not a history — a squeeze that has gone stale or already resolved drops off
      here even though it's still recorded. For the full historical record, see Squeeze History or
      Performance." (2) The Documentation tab's "Making your way around the web application" guide
      (docs/_guides_content.js) — reworded the Scanner Report row in the tab-overview table to the same
      effect, and updated the Performance row to note it covers "setups no longer showing on the Scanner."
      While in that file, also fixed the SAME stale numbers already caught and corrected in the
      plain-English docx earlier today (P-12 above) but missed in this second copy: the "What is the
      squeeze?" guide's Real-squeeze check said "tighten by at least 30% ... under 70%" (live threshold is
      HVF_TIGHTNESS_MAX=0.35, i.e. under 35%) and its Publish gate said "at least 70/100" (live
      MIN_PUBLISH_QUALITY=25) — both corrected to match. Rebuilt all 20 guides via docs/_build_guides.js
      (confirmed via the script's own output — all 20 .html/.docx pairs + the manifest regenerated). NOTE:
      docs/_guides_content.js and docs/guides/ are gitignored, so these live on disk but are NOT in git.
      Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - General
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-06 Colour of card header titles needs to be clear enough to be different from rest of text e.g. Transaction evidence -- User decision 2026-08-07: change all cards. Changed `.card h4` from `var(--muted)` to `var(--fg)` with bold weight, sitewide. 12 tests passed. Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Standards and Guidelines
------------------------------------------------------------------------------------------------------------------------------------------------

    --------------------------------------------------------------------------------------------------------------------------------------------
    Standards and Guidelines (IT IS IMPERATIVE THAT ALL THESE ARE APPLIED AND CONTINUE TO BE IMPLEMENTED)
    --------------------------------------------------------------------------------------------------------------------------------------------

    * P-02 Global/Docs - "Hunt Volatility Funnel" and "HVF" and "funnel" must NOT appear in any document — each must appear as "The Squeeze" -- Reworded all prose occurrences (user 2026-08-08 "p-02 reword must be complete"): docs/SQUEEZE_METHOD.md (9), docs/DECISIONS_AND_WEIGHTING.md (5), docs/PROJECT_ARTIFACTS.md (1), BACKLOG.md (4). "funnel" (the pattern) → "squeeze"/"The Squeeze" reworded for grammar rather than blind-substituted. DELIBERATELY PRESERVED (not prose, renaming would break references or misstate facts): code identifiers containing hvf (get_hvf_signal/_mtf, hvf_clean.detect_hvf, price_action.hvf_weight, ig_shim.place_hvf_order_from_sig, check_hvf_invariants, run_hvf_report, hvf_timeframe, instrument_dossier._hvf_rules_block), the skill names ah-hvf-analysis/ah-hvf-orders/ah-hvf-report, the asset filename img/hvf_funnel.png, and "Francis Hunt"/"Hunt" (the person who devised the method). Re-scan confirms zero prose "funnel"/"HVF"/"Hunt Volatility Funnel" remain. This is a standing standard, kept in this section; treat new violations as regressions. Claude. [Completed]
    * P-12 Update *.docs files in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs (How_The_Trading_System_Works_PlainEnglish)
      -- Claude 2026-08-11: read the doc (pandoc dump) and cross-checked every quoted number/threshold
      against the actual live code rather than assume it was still accurate. Found and fixed two real,
      verified drifts: (1) "Publish gate: Pattern quality must be at least 70/100" — config.py's
      MIN_PUBLISH_QUALITY is 25, not 70 (changed 2026-06-22, "promote middling-quality clean funnels" per
      its own changelog); the doc was quoting the pre-change value. (2) "Real squeeze: tighten by at least
      30% ... under 70% of its starting width" — hvf_clean.py's actual live threshold is
      HVF_TIGHTNESS_MAX=0.35 (its own code comment: "RW's compression ceiling, stricter than AH's 0.70
      convergence") — the doc was describing an older, looser convention the live detector no longer uses.
      Corrected both in the table text, updated the cover date to note the correction, edited the docx XML
      directly (unzip → merge_runs → replace → rezip) to preserve all existing formatting/images/layout,
      validated (validate.py: 86 paragraphs before/after, all checks passed) and visually verified by
      rendering to PDF/JPEG and reading every page. Verified everything else quoted (R:R >= 3:1, 60-bar
      freshness window, ranking order R:R > readiness > quality) already matches the code — no other
      changes needed there. KNOWN REMAINING GAP: the page-2 "Hunt Volatility Funnel" diagram is a static
      image with the SAME stale "must tighten >=30%" claim baked into its pixels (not editable text) —
      out of scope for a text-only fix; the diagram would need to be regenerated to match the corrected
      35% figure now in the table beside it. Claude. [Completed — text corrected and verified; the one
      diagram image still needs manual/regenerated correction, flagged above]

    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "User Guide: Making your way around the web application." -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "User Guide: What is the squeeze?" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "User Guide: The importance of volume" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "User Guide: Adding IG credentials" - how to get API key etc -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "User Guide: Configuration" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]

    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "User Guide: Feature - Tab Visibility" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]

    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Support: Adding Users" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Support: Adding Markets" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Support: Adding Instruments" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Support: Configuration tabs" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Support: Posting reports to X" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]

    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: System Logs" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: Scheduled Jobs" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: Batch Activity" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
      -- Claude 2026-08-11: user noticed the app's snapshot showed "generated 01:27:06" (local) but Batch
      Activity had no matching row for that morning. Root-caused: (1) the actual overnight price refresh
      DID run — price_audit_log confirms 04:52 UTC, 1421 tickers, 29,346 bars written — but (2) the
      01:27:06 timestamp was a DIFFERENT thing, hvf_web/server.py's in-process 12h snapshot-rebuild loop
      (_refresh_loop/_do_rebuild), which never called append_batch — only the manual "Refresh data" button
      did. User directive: "any batch activity with frequency less than 6 times per day should write to
      the batch log." Backfilled a retroactive web_batch_activity row for the 2026-08-11T00:27:06.839164Z
      rebuild (1421 instruments) via direct SQL. Then wired append_batch() into every qualifying <6x/day
      job that wasn't already logging: the automatic snapshot-rebuild loop (server.py), and 9 run_session.py
      branches (AUS/UK/US Open, Session Close, Weekend Review, Sunday Pre-Open Commodity Scan, AUS/UK/US
      HVF Watch — the *_MONITOR/POSITION_MONITOR branches deliberately excluded, ~5min cadence = well over
      6x/day), run_hvf_orders.py, run_daily_report.py, run_uk_morning_brief.py, working_orders_report.py,
      run_cot_report.py, run_diagnostics.py (Daily Diagnostics + Sunday Readiness, shared script), price_audit.py
      (Price Data Refresh + Price History Audit, shared script — same mode/args at both cron times, so the
      logged event can't distinguish which job triggered it by name), run_data_quality_audit.py, and
      supabase-backup.yml (added actions/checkout + setup-python, previously pure-shell pg_dump with no
      Python step at all, plus a final step that logs the backup size). All 10 edited .py files verified
      with py_compile; full existing test suite (test_server_auth.py, test_migrate_secrets.py,
      test_instruments.py, test_setup_cronjobs.py, test_performance.py, test_bounce_monitor.py,
      test_scanner_report_email.py, test_web_users.py — 76 tests) still passes; no dedicated tests existed
      for the 10 edited scripts (none broken). [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: Funding X publications" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: Patching software e.g. choco" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]

    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: Change Control" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: Version History" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: User Management" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]
    * P-12 Create a document in C:\Users\eahin\OneDrive\ClaudeCode\EndToEndTrading\docs for "Operations: Other" -- Delivered as a guide in the Documentation tab (P-13): written in docs/_guides_content.js, built to HTML in docs/guides/ (gitignored), role-gated (User Guides to all logins; Support/Operations to staff). Claude. [Completed]

    * P-13 Create a new tab for "Documentation" links (available to Admin \ Support logins) - to right of "How it works (System)" -- Added a "Documentation" tab immediately right of "How it works (System)". Visible to any logged-in user; the content is role-filtered. New server routes GET /api/guides (role-filtered list) and GET /api/guides/<slug> (serves the guide), both gated by web_users role: User Guides (access "login") to any logged-in user, Support/Operations guides (access "staff") to admin OR support only — enforced server-side, not just hidden in the UI. Documents are grouped into logical numbered categories (User Guide / Support / Operations) shown as clickable cards, styled after guardian-one.org/risk-management.html (hero eyebrow+title+lead, then numbered sections). SWITCHED delivery from .docx download to in-app HTML (user 2026-08-08: downloading each doc was slow) — _build_guides.js now also emits a self-contained HTML fragment per guide (escaped at build time) and the manifest carries subtitle + .html; clicking a card fetches and renders it instantly in-app (no download), with an "← All documents" back link. Auth travels in the X-Auth header (fetch), so no token ever appears in a URL. NOTE: docs/guides/ + docs/_guides_content.js are gitignored, so guides must be built on the server machine (run docs/_build_guides.js there). 90 tests passed (4 new: /api/guides 401 without login, staff guides filtered out for a plain user, included for admin, and the file route 404s a staff guide for a non-staff user). Claude. [Completed]
    * P-13 All User Guide in "Documentation" tab to be available to all logins. -- Satisfied by the same implementation: the Documentation tab is a login-required tab that is NOT admin-gated, so every logged-in user sees it; User-Guide-category guides (access "login") are returned to all logged-in users, while Support/Operations remain staff-only. Verified by the access-filter tests above. Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - BUGS
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Scheduler
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-08 The job names look peculiar, please check them out -- Claude 2026-08-07: checked, didn't assume — traced the Scheduled Jobs tab's stats pipeline (hvf_web/scheduled_jobs.py get_jobs()) and found it caches GitHub Actions run stats PER WORKFLOW FILE. "Price Data Refresh" (04:30 UTC) and "Price History Audit" (23:00 UTC) both dispatched trading-price-audit.yml, so the tab showed them with byte-identical Last status/Last time/Executions/Failures despite running 18.5 hours apart for different reasons — reads exactly like a stray duplicate. Split "Price Data Refresh" onto its own new trading-price-refresh.yml (same script) so its history is genuinely its own. A regression test that scans setup_cronjobs.JOBS for any two jobs sharing a workflow file caught ONE more real instance — "Commodity Monitor AM"/"Commodity Monitor PM" — but left that one alone: the workflow's own header explains it's ONE conceptual monitor split into two cron windows "to cover the gaps between session monitors", not two different operations, and the AM/PM suffix already signals that pairing (unlike "Refresh" vs "Audit", which don't read as related) — so it's an intentional exception, allowlisted in the test with the reasoning inline. No other job names/workflow assignments looked mismatched. 6 tests passed (test_setup_cronjobs.py, incl. 1 new). Claude. [Completed]
    * P-09 Allow all charts to have multi select and cross filter -- Claude 2026-08-07: multi-select already worked (shared [data-fk] dispatcher), but the 4 charts didn't cross-filter each other (each counted the FULL unfiltered job list, not the others' current selection) and "Recent failures" was wired to a dead filter key (jf_none — never checked by the table filter, so clicking it silently did nothing). Rewrote paintJobs() with the same brushed byX(exceptId) pattern used on Squeeze History/Back Test (each chart counts rows passing every OTHER chart's selection) and gave "Recent failures" a real per-job filter (jf_job, matched against job title). 55 tests passed. Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Volume Score (new)
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Instruments
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-08 Content - Create a new tab called "Instruments" -- Claude 2026-08-07: built. New public "Instruments" tab reusing the SAME DATA the Scanner already loads via /api/records (extended server-side with wk52_low/wk52_high — see below), so no extra fetch for the main table. Doc/gsec/table layout matching Squeeze History (the "New UI Settings" convention — confirmed by reading its own trail of completion notes back to 20260710/20260718/20260731, which describe it as exactly this doc/gsec/table-or-fcard layout). 54 tests passed (incl. 6 new: test_instruments.py). Claude. [Completed]
    * P-08 Content - Include the popular cards across the top location, market, sector and anything else worthwhile -- Claude 2026-08-07: Location/Market/Sector/Status cards, brushed cross-filter (same pattern as Squeeze History's chart strip). Added a 4th "worthwhile" card, Direction — but only once logged in, since Direction is one of the columns required to be hidden pre-login (showing it in aggregate on a filter card would leak the same information a hidden column is meant to hide). [Completed]
    * P-08 Content - Each card can have multiple filter and these cross filter the other cards -- Claude 2026-08-07: multi-select, brushed (each card counts rows passing every OTHER card's selection, not its own) — same inf_* convention as sqf_/pff_/ordpf_ elsewhere, wired into the shared [data-fk] click dispatcher. [Completed]
    * P-08 Content - Include the column names in a table name, market, sector, direction, quality, 52wk low, 52wk high, r:r, rvol, vwap, atr, volume score, ticker -- Claude 2026-08-07: all 13 columns, in this exact order. 52wk Low/High are newly computed server-side — added _snapshot_52wk() to hvf_web/server.py (trailing-365-day min(low)/max(high) per ticker from price_history, reusing the existing _perf_bars bulk-fetch helper and the same generated_utc cache pattern as RVOL/VolumeScore) and merged into /api/records for both the authed AND logged-out payloads (public, unlike RVOL/Quality/R:R/VolumeScore). [Completed]
    * P-08 Content - Anyone not logged in cannot see direction, quality, r:r, rvol, vwap, atr or volume score -- Claude 2026-08-07: all 7 restricted cells use the site's existing ob() teaser convention (same "•••" placeholder already used on the Scanner/Performance tables) rather than removing the columns — consistent with how the rest of the site already teases logged-out visitors. Note: /api/records already exposed "direction" to logged-out users for the Scanner's own teaser (5 public columns, unchanged, out of scope to alter); Instruments simply never displays that field pre-login regardless, satisfying this requirement without touching Scanner's existing public contract. [Completed]
    * P-08 Content - Provide a history of the funnels (perhaps separate to the table) - I'll take your guidance on this for UI and UX -- Claude 2026-08-07: added a separate "Funnel history" mini-table below the main table — the 30 most recent squeeze lifecycle events (developing/ready/triggered/outcome), reusing the existing /api/squeeze-history endpoint (confirmed it already has no server-side auth check, so this adds no new data exposure) and filtered by the SAME Location/Market/Sector/Direction selections + name search as the table above. Direction/Return cells use the same ob() teaser. [Completed]
    * P-08 Content - Include anything else on the page that you deem relevant -- Claude 2026-08-07: added a Status card (has_signal-derived) as a 4th always-public filter, and a "Log in to see the trading signals and open a detail report" hint in the page lede for logged-out visitors. [Completed]

    * P-08 Filters - Allow instrument name search on the table - similar to all other name searches we have implemented -- Claude 2026-08-07: same name/ticker search box + oninput pattern used on every other table (Squeeze History, X Posts, System Logs, ...). [Completed]
    * P-08 Availability - Available to all (even without a login) -- Claude 2026-08-07: added "instruments" to PUBLIC_TABS (the list tabAllowed() checks before requiring a login) and to TABS; deliberately NOT added to ADMIN_TABS. Verified: tabAllowed('instruments') is true with no AUTH. [Completed]
    * P-08 Availability - Only people logged in can see the detail reports when clicking on instrument name -- Claude 2026-08-07: clicking a name calls openDetailFrom('instruments',ticker) (the same rich instrument detail the Scanner already uses — full chart + funnel schematic, not a new view) only when AUTH is set; logged out it calls showLogin() instead, same pattern as every other gated detail link on the site. [Completed]
    * P-08 Format - Use the "New UI Settings" layout and formatting. -- Claude 2026-08-07: doc/gsec/table layout (see first Content note above). [Completed]
    * P-08 Format - Add "Instruments" to configuration - visibility - - DEFAULT IS VISIBLE -- Claude 2026-08-07: added to the Tab Visibility admin screen (grouped under "Instrument Operations" alongside Scanner/Performance) and deliberately NOT added to DEFAULT_HIDDEN, so it defaults to visible and is only hidden if an admin explicitly turns it off. [Completed]
    * P-08 Format - to the LEFT of "Squeeze History" -- Claude 2026-08-07: nav button placed immediately before Squeeze History's. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-04 Add new category in the user configuration leverage to match https://www.ig.com/uk/charges/margin if required -- Checked, Claude 2026-08-07: this is a re-file of the item already audited and completed in 20260805-ToDo-Claude.txt L165 — LEVERAGE={fx:30,equities:5,commodities:10,indices:20} still matches IG UK's four retail margin groups (Indices/Forex/Commodities/Shares) exactly. Still no additional category required; no code change. [Completed — confirmed still accurate, no action needed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab : Best Settings
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-10 (user 2026-08-12) Create two more cards in "Best settings" - best settings for >250 trades, then >500 trades. -- Mirrored in the 20260813 register; the dedicated large-population annual search now supports both thresholds. Full suite: 165 passed. Claude. [Completed]
    * P-03 3 of the 4 cards are using the same sector - is there anything else that gives more choice? --
      Claude 2026-08-11: read renderBestCombo()'s card-selection algorithm rather than guess. Each of the 4
      cards (Balanced/Growth/Defensive/Broad evidence) is picked via choose(), which requires a candidate to
      differ from every already-chosen card by >=2 of 9 dimensions (rr/quality/volume-score/rvol/vwap/atr/
      stake/max-open/scope) — scope (market/sector/mcap band) is only ONE of those 9, so if one sector
      dominates every objective's ranking (return, drawdown, consistency), 3+ cards can trivially satisfy
      "differs by >=2" via different R:R/Quality/etc combinations while all keeping that same winning
      sector. Fixed: choose() now tries a NEW first tier — same >=2-dimension bar, but ALSO requires a scope
      different from every already-chosen card — before falling through to the original two tiers
      unchanged. This is a preference, not a requirement: it never trades away return/drawdown/consistency
      quality to force variety, it just prefers a scope-diverse candidate when one already clears the same
      bar. No Python-side tests cover this client-JS algorithm; full suite (114 tests) still passes,
      confirming no unrelated regression. Claude. [Completed]
    * P-03 If one of the 4 cards (Balanced, Growth, Defensive or Broad evidence) is clicked, change the contents of table below to use this cfg -- User decision 2026-08-07: detail follows whichever is clicked. Refactored renderBestCombo so the four choice cards call selectBestChoice(label); the detail card (metrics grid + Transaction evidence) below now re-renders for whichever option was last clicked, defaulting to Balanced, with the clicked card highlighted. The funded replay proof is computed lazily per choice (only when actually viewed) to avoid 4x the replay cost on every render. 34 tests passed (2 rewritten to match the new per-choice call). Claude. [Completed]
    * P-04 Look at multiple rows for one instrument on the same day e.g. "Domino's Pizza Enterprises Limited" -- User decision 2026-08-07: dedupe, best outcome wins. Collapsed same-ticker/same-day rows in Best Settings' input population (renderBestCombo) to one row, keeping whichever recorded the better return; scoped to Best Settings only (not Squeeze History, which remains the full trigger log). 34 tests passed. Claude. [Completed]
    * P-04 Allow merge transactions - as we use in fees and IG account [Cancelled]
    * P-05 Transaction evidence needs to contain columns to confirm the performance e.g. wallet, no. open transactions, net gain -- User decision 2026-08-07: build it now. Confirmed genuinely outstanding (not a duplicate of the similarly-worded, already-completed 20260805 L142 item, which only moved the shared Wallet/Position/Max-open control panel above the title). Added a Wallet column (the model wallet immediately before each decision — the replay already tracked this per row, just wasn't displayed) and a Net gain column (£ profit/loss for placed trades, computed from stake × that trade's return%; blank for missed trades). "Open after decision" already covered the open-transactions-count part of the request. 34 tests passed. Claude. [Completed]

    * P-02 Missed transactions Hide \ Show is difficult to see - please resolve -- Claude 2026-08-11: was a
      bare <fieldset>/<legend> with two plain unstyled radio+label pairs at 10-12px, no border or background
      to separate it from the surrounding text — easy to miss in the Transaction evidence header row.
      Replaced with a two-button segmented pill toggle (border, rounded, active state filled with
      var(--accent)/white text) matching the site's existing .pill/.pill.active pattern used elsewhere
      (Configuration nav, Performance's #pf-pills). No Python-side tests cover this render path (client-only
      JS); full suite (114 tests) still passes, confirming no unrelated regression. Claude. [Completed]
    * P-03 Move "Replay model card" to above "Best settings for annual profit" -- Claude 2026-08-11:
      verified rather than assumed — `git log -p -S"pf-shared-model"` shows the "💷 Replay model" card was
      already moved to its current position (shared across Best settings/Summary/Let winners run/Advanced
      analysis, hidden only for Back Test) on 2026-08-05, two days before this change-request file existed.
      It sits before the Performance <h1> and every sub-tab panel in DOM order, so it renders above "Best
      settings for annual profit" whenever that panel is shown. No code change needed — already satisfied
      by an earlier refactor; the change-request line itself was just never marked. Claude. [Completed —
      already done, confirmed via git history, no redundant work performed]
    * P-06 On all other tables BEAR and BULL are colour coded i.e. RED and GREEN -- Squeeze History's Dir column was the odd one out (rendered as plain text, no other table was). Added the same `<span class="tag bull/bear">` colour coding used everywhere else. 34 tests passed. Claude. [Completed]
    * P-06 Remove table containing "Equity	Opening stake	Ending equity	Net P&L" -- Removed the single-row summary table (and its now-unused row-building code) from Best Settings' Balanced recommendation detail card; the same figures already appear in the metrics grid above it. 34 tests passed. Claude. [Completed]
    * P-06 "Balanced recommendation detail — ranked by return …. " looks lost - is it better to have all 4 cards in here?" -- User decision 2026-08-07: detail follows whichever is clicked (same fix as the L118 item above — one refactor covers both). [Completed]

    * P-04 With so few bars in "Market · achievable P&L" and "Quality · achievable P&L", these maybe better not using charts e.g. PIE chart [Cancelled]
    * P-04 With so few bars in "Market · achievable P&L" and "Quality · achievable P&L", it may be better to add other charts stacked e.g. VWAP, ATR, RVOL range -- User initially chose "skip for now", then asked to resume, Claude 2026-08-07: added RVOL/VWAP/ATR achievable-P&L charts alongside Market/Sector/Quality (6 total). RVOL bands match the existing rvolCell() convention used everywhere else (≥2 spike / ≥1 above normal / <1 thin) rather than inventing a new one; VWAP/ATR use their existing true/false/null values with readable labels instead of new logic. 12 tests passed. Claude. [Completed]
    * P-04 Whilst adding extra charts, keep them all on one row unless told otherwise -- User decision 2026-08-07: one row for charts. Switched the achievable-P&L chart strip to the site's existing "chart strip never wraps" convention (the same `.viz` rule already used by Scanner/Performance Results): flex-wrap:nowrap, cards shrink to fit (flex:1 1 auto;min-width:0), overflow-x:auto only in the extreme case where even shrunk cards can't fit. All 6 charts (Market/Sector/Quality/RVOL/VWAP/ATR) now stay on one row. 12 tests passed. Claude. [Completed]
    * P-06 Remove "⚖️ Leverage & the wallet: leverage does not change the £ or % return; it changes the margin each position ties up. Max open …" -- Removed exactly this text from the Replay model card. 34 tests passed (also updated a regression test that had locked in the old wording as required). Claude. [Completed]
    * P-05 In "💷 Replay model" set Max position size 5% Max open positions 50" -- Max open positions already defaulted to 50 (no change needed); changed Max position size default from 2% to 5%, including the JS fallback defaults used before the DOM value loads. 34 tests passed. Claude. [Completed]
    * P-06 Is there any point in "Back Test" if we have "Transaction evidence" - do NOT make changes, it is a question for now!!!
      -- Claude 2026-08-11 (answer, no code change — read/verified against the actual index.html logic, not
      assumed): they answer different questions and aren't redundant. Back Test's entire row population is
      filtered by _pfMatchesCurrentConfig() through your SAVED My Trading Filters (R:R/Quality/VolumeScore/
      RVOL floors, VWAP/ATR requirements, instrument-value band, saved market/sector scope) plus whatever
      Date Window is set — its own code comment says outright "Back Test applies the complete saved
      configuration." So its job is: given the filters I've configured, how did the qualifying population
      perform, broken down by market/sector/quality/RVOL/VWAP/ATR/location/direction/outcome/month, with an
      aggregate wallet-compounding summary (£ start -> £ end, N taken, M skipped). Transaction evidence is a
      chronological, trade-by-trade audit of ONE specific candidate strategy (one of the 4 Best Settings
      cards, or Let Winners Run — each with its OWN searched combo, not necessarily your saved filters at
      all), explicitly marking every row Placed or Missed with a stated funding reason.
      User then asked: "so if the same thresholds were used in both would they mirror each other?" — tested
      rather than assumed. Answer: not automatically, even with identical R:R/Quality/VolumeScore/RVOL/VWAP/
      ATR numbers, for three concrete reasons found in the code: (1) Best Settings' candidate search
      (renderBestCombo) deliberately runs against the FULL, unwindowed dataset by design — its own comment:
      "Do not silently train it on the descriptive Results tab's date, saved-setting or chart selections:
      those would bias the recommendation toward today's setup" — whereas Back Test's population is whatever
      Date Window + saved scope you currently have set; (2) Back Test excludes anything tradeVisible() hides
      (admin Trade Filters / disabled markets), Best Settings' candidate search does not call that check; (3)
      the two wallet-replay functions compute a trade's exit date differently — Back Test's _pfWalletLedger
      always derives it as trigger-date + days-open, Transaction evidence's _combReplay prefers the trade's
      actual recorded exit_date and only falls back to the computed one if missing. Matching the six floors
      AND the date window AND the scope would bring the qualifying trade list into line; the wallet £ figures
      would then be close but not guaranteed identical because of (3). NOT marked Completed at the user's
      request — this is an explanation, not a resolution, and the user has said they don't yet fully
      understand it; leaving open for further discussion rather than closing it out. Claude. [Open — answer
      given above, no code change, user still working through it]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab : Back Test
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-09 As with all tables, allow sort by column header -- Claude 2026-08-07: checked, didn't assume — the th[data-pf] click handler and pfSortK/pfSortDir already existed and updated correctly on click, but were silently ignored: the wallet model is always on (pfwOn hardcoded true since 2026-08-01) and the row-order line unconditionally forced strict chronological order whenever pfwOn was true, so clicking a header changed state that was never read. Fixed by always computing the wallet ledger (needed for the Stake/Net/Equity/Open columns, which only compound correctly in chronological order internally) but using genSort(selT,pfSortK,pfSortDir) for the DISPLAYED row order — the ledger's own internal sort is independent of display order, so this is safe. Sorting now actually works. 55 tests passed (incl. 1 new). Claude. [Completed]
    * P-09 Default sort on table is "TRIGGERED" ASCENDING -- Already the default (pfSortK="trig_date", pfSortDir=1, set 2026-08-01) — but see the item above: the default was cosmetically correct while every OTHER sort choice was silently ignored. Confirmed genuinely working now that the fix above landed. [Completed]
    * P-09 Move MCAP to far right -- Claude 2026-08-07: moved the MCap header + cell from right after Market to immediately before Sector/Ticker — as far right as possible while respecting the site's existing "Ticker always rightmost, Sector just before it" convention (P-11a, used on every other table). [Completed]
    * P-09 Use colour codes for ticks and crosses i.e. RED and GREEN -- Claude 2026-08-07: added a reusable _tickCross() helper (green ✓ / red ✗, matching var(--bull)/var(--bear) used everywhere else) and applied it to the VWAP and ATR columns, which previously rendered plain uncoloured ✓/✗ text. Left their existing (unobfuscated) logged-out visibility unchanged — out of scope for this item. 55 tests passed. Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab : Advanced Analysis
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-05 Move all "Let winners run" objects into it's own page with a button next to "back test" -- Found and fixed a regression, Claude 2026-08-07: this was already built and marked [Completed] on 2026-08-04 (20260805-ToDo-Claude.txt L154), but a same-day follow-up commit (bb23c0f, later that evening) accidentally removed the pill button and silently redirected pfPanel('run') to 'settings', with no change-request note explaining an intentional removal — and a regression test was edited in the SAME commit to lock in the broken state, which is why it went unnoticed. Restored the "🏃 Let winners run" pill next to Back Test (gated to Silver/Gold admin, same as before) and removed the redirect; the dedicated page, its illustration and its render logic were never actually deleted, only unreachable. Updated the two regression tests that had encoded the broken behaviour as expected, with a note explaining why. 34 tests passed. Claude. [Completed]
    * P-06 In "Transaction evidence" add column for difference between "Sell at target" and "Let run" -- Both values were already computed and both already had columns; added a Difference column (colour-coded like the others) that only appears in the "Let run" view, and corrected the empty-state colspan to match. 34 tests passed. Claude. [Completed]
    * P-07 Above "Transaction evidence" add: All the charts used in "Back test" above "Every recorded trigger" i.e. location through to month -- Claude 2026-08-07: renderDecisionProof() (the shared Transaction evidence renderer used by both Let Winners Run and Best Settings) already had its own achievable-P&L chart strip above the table (Market/Sector/Quality/RVOL/VWAP/ATR, added earlier this session for P-04). Extended it with Back Test's own 6 remaining charts — Location, Direction, Outcome, Win/Loss, Days Open and Month — using the same field names and bands Back Test itself uses (locName, _doBand, PF_BE win/loss split), so all 8 of Back Test's charts now sit above Transaction evidence too, in the same order. 12 tests passed. Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab : All
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-03 Reconcile all reports on all the tabs to make sure they back each other up for evidence --
      Claude 2026-08-11: an exhaustive number-by-number audit of every report is open-ended, so investigated
      the most likely concrete divergence points rather than a vague pass. (1) Win/Loss/Break-even
      threshold (PF_BE): checked every place perf is bucketed into Win/Loss across Best Settings, Back Test,
      Summary and Transaction evidence — all consistently use PF_BE=0 (some via the named constant, some via
      the literal >0/<0, which is mathematically identical) — already reconciled, no fix needed. (2) Found a
      real one: the same-ticker/same-day double-counting fixed for Best Settings alone on 2026-08-07
      ("Domino's Pizza Enterprises Limited" — the scanner's overlapping daily-30/60/90/180/240+weekly
      lookback windows can trigger the same ticker on the same day more than once) was never applied to the
      shared WIN/PERF_DATA arrays every OTHER Performance report reads from (Back Test, Summary, Let winners
      run) — only to a local copy inside renderBestCombo(). So Best Settings' now-deduped counts could
      silently disagree with Back Test/Summary's still-inflated counts for what should be the same
      underlying trades. Fixed at the single shared source instead of patching every consumer: added
      _dedupeSameDayRows() and applied it where WIN and PERF_DATA are first populated from their API
      fetches, so every report built from either array is now consistently deduped (Squeeze History
      deliberately excluded, unchanged — it's documented as "the full trigger log" by design, a separate
      /api/squeeze-history endpoint entirely). renderBestCombo's own local dedup is now redundant against
      this but left in place — deduping already-deduped rows is a harmless no-op. No Python-side tests cover
      this client-JS path; full suite (114 tests) still passes. Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Other
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Data
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - How it Works Tab
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Instrument Details - Content
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-03 Add the latest relevant visual from @ratedmarkets on X to the HVF Details Report for the same instrument.
    * P-03 Add the latest relevant visual from @InvestingVisuals on X to the HVF Details Report for the same instrument.
      -- Claude 2026-08-11: already fully built — corrected an initial wrong claim. First check only looked at
      x_publish.py (the official X API v2 used for POSTING, correctly free-tier-limited on reads — "X's free
      tier 403s that read", its own comment) and concluded reading was blocked; user pointed out a
      notable_investors-style table existed, which was the right challenge. social_monitor.py reads via public
      Nitter RSS feeds instead (no X API/key needed at all) — @ratedmarkets (added 2026-08-06, "so HVF Details
      can show current instrument-matched visuals" — this exact request) and @investingvisual (the live handle
      for "Investing Visuals") are both in TRACKED_ACCOUNTS, with each account/ticker's latest durable x.com
      post URL kept current in the notable_investors table. hvf_web/server.py's /api/links/<ticker> already
      returns a "visuals" array for both accounts, and hvf_web/index.html's paintXVisuals() already renders a
      "Relevant X visuals" card in the HVF Details view, embedding the real tweet via X's public widgets.js
      (URL-validated to x.com/twitter.com status links only). Confirmed via git blame it was built 2026-08-06
      under this same ChangeRequest line's original wording — no code change needed. Claude. [Completed —
      already done, confirmed by reading the actual backend+frontend code, no redundant work performed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Scanner Report Tab
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-07 Email the scanner report each day to the account holder -- Claude 2026-08-07: added run_scanner_report_email.py — for every ENABLED web user with a registered email, sends today's Squeeze-only setups (has_signal, the Scanner's own default view) from hvf_web/snapshot.json, enriched with RVOL/VolumeScore exactly like /api/records, filtered by that user's own saved Quality/R:R/RVOL floors (Configuration -> Preferences -> "Scanner Report filter defaults") if they've set any. One user's bad email/send failure never blocks the rest; each successful send is logged to their account activity and the run as a whole to Batch Activity. Reused the existing trade_email.send_simple_email provider plumbing (Resend/Yahoo) — no new email infrastructure. IMPORTANT — scheduling: hvf_web/snapshot.json and data/web_users.json are LOCAL, gitignored files that only exist on the machine running the Flask server (confirmed by reading hvf_web/build_snapshot.py and hvf_web/scheduled_jobs.py, not assumed) — a GitHub Actions runner has neither, so unlike the other scheduled jobs this can NOT be wired into the cron-job.org -> GitHub Actions pipeline; it needs a LOCAL daily scheduler (e.g. Windows Task Scheduler) on the same machine as the web server, run after the day's snapshot rebuild. 8 new tests (including a regression test for a malformed-HTML bug caught and fixed during review — a style attribute's closing quote landed before extra CSS was appended). 44 tests passed (full run: performance + web_users + server_auth + scanner_report_email). Claude. [Completed — scheduling is local-only per the note above, not yet wired to an actual OS scheduler]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - My Pre-orders
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - My Pre-orders (New)
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Pre-orders to My IG
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Main Page
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - X Posts
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - IG Account Tab : Amend
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab : Summary Page
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab : Results Page
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Performance Tab : What separates the winners
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Operations Tab : System Logs
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Operations Tab : Batch Activity
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Operations Tab : Change Requests
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Data
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Operations Tab : User Management
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-10 Add a button in User Management for "Add User" (ad-hoc request, user 2026-08-07). -- Added a "+ Add user" button that opens a modal (name, email, subscription, admin checkbox) and creates the account directly via a new admin-only `action:"create"` on POST /api/users (`web_users.admin_create_user`), gated the same way as every other admin branch on that route. Matches the existing "approve request" security model: the account is created LOCKED (unusable random password) and enabled, and a password-reset-code email is fired immediately so the new user can set their own password — no admin ever sees or sets a real password. 32 tests passed, including 5 new ones covering admin_create_user (locked/enabled account, duplicate-name and invalid-email rejection) and the endpoint's admin gating (non-admin 403, admin success). Claude. [Completed]

    * P-04 Add a new role for Support - if it is easier to have a role that is "Admin \ Support \ User" to manage less data that is fine. -- User decision 2026-08-07: Support = read-only System Logs, Batch Activity and Scheduled Jobs, nothing else admin-only. Added a `support` field independent of `admin` in web_users.py (is_support/set_support), gated the 3 endpoints to admin-OR-support, carved SUPPORT_TABS=[batch,syslogs,jobs] out of the front-end ADMIN_TABS check, exposed is_support via /api/me, and added a Support checkbox in User Management next to Admin. Verified Support can reach the 3 endpoints and NOT Version History or User Management; verified a plain guest gets 403 on both. 38 tests passed (3 new: independence of the two flags, set_support, and endpoint-gating for support/non-support). Claude. [Completed]

    * P-01 (user 2026-08-08) Support cannot be selected when creating a NEW account -- the "+ Add user" modal has an Admin checkbox but no Support checkbox, so a new account can only be made Support by creating it then toggling the row. -- Added a Support checkbox to the Add-user modal (appAddUser) and carried `support` through addUser() -> POST /api/users action:create -> server.py -> web_users.admin_create_user (new `support` param, persisted on the account like `admin`). The existing per-row Support toggle was already working; this closes the gap at creation time. 82 tests passed (2 new: server forwards the support flag; admin_create_user persists support=True). Claude. [Completed]
    * P-06 (user 2026-08-08) The Admin and Support checkboxes in the User Management row do not need text next to them - the column header already labels them. -- Removed the redundant " Admin"/" Support" text from the row checkboxes in renderUsers(), centred the checkbox in its cell, and moved the descriptive wording into each checkbox's hover title so nothing is lost. Column headers (Admin / Support) already label them. Claude. [Completed]
    * P-01 (user 2026-08-08) Do not allow a clearly-invalid email address to be entered for an account -- e.g. "c.d.mason@gmail,com" (comma) was accepted because validation only checked for "@". -- Added pragmatic email validation (single @, dotted domain, no spaces/commas; NOT full RFC 5322) on all four entry points: front-end validEmail() used by the Add-user modal and the public account-request form, and back-end web_users.valid_email() used by admin_create_user and add_request. The comma address is now rejected at entry. 82 tests passed (1 new: valid_email accepts good / rejects comma+malformed; plus a create-with-comma rejection test). Claude. [Completed]
    * P-06 (user 2026-08-08) Fix the email on the existing "Carl Mason" account -- stored as "c.d.mason@gmail,com", should be "c.d.mason@gmail.com". -- Corrected in data/web_users.json via an atomic rewrite (tmp + os.replace, matching web_users._save). NOTE FOR USER: there is ALSO a separate pending account request under the name "Carl" that already holds the correct address c.d.mason@gmail.com — so Carl currently exists twice (created account "Carl Mason" + pending request "Carl"). Left the pending request in place for you to Approve or Reject as you see fit; say the word and I'll remove the duplicate. Claude. [Completed]
    * P-10 (user 2026-08-08) When a user is not logged in, show a clear Log in / Create account control next to the "Dark" theme button (top right). -- Added a "Log in / Create account" button (id="loginbtn", accent-styled) immediately right of the Dark theme button; it calls showLogin() to open the existing login/create-account panel. It is mutually exclusive with Log out: shown when logged out, hidden once authenticated (wired into the same AUTH toggle as the logout button, and re-shown if the session expires). Claude. [Completed]
    * P-10 (user 2026-08-08) When an admin APPROVES a pending account request, the new user should get an email so they know -- currently only the "+ Add user" path emails a password-setup code; "Approve" creates the locked account silently, so the person is never notified. -- The /api/users approve branch now fires request_reset_code(target, email_for(target)) after a successful approve_request, best-effort in a try/except so an email failure never undoes the approval — exactly mirroring the create path. Updated the front-end approve confirmation wording from "They set their password via 'Forgot password?'" to "…emails them a setup link at their registered address so they can set a password." 83 tests passed (1 new: approving a request calls request_reset_code with the registered email). Claude. [Completed]
    * P-10 (user 2026-08-08) Add an admin-only "Set temporary password" affordance in User Management, so a new/locked user can be given a credential out-of-band when outbound email is not configured -- BUT only for accounts that do NOT have IG credentials (an IG-linked account can place real trades, so force-setting its password is too risky; it must use the email-based reset), and it can only be used by admin people (already enforced: /api/users is admin-gated). -- Added a "Set temp password" button in the User Management Password-Complexity cell, shown ONLY for accounts with no IG credentials; IG-linked rows instead show a muted "🔒 IG-linked — use email reset" note. Clicking opens an admin modal (with a Generate button for a strong password) and POSTs action:"set_temp_password" to /api/users, which sets it via web_users.reset_password (email-only path, min 4 chars, doesn't disturb role/subscription). The endpoint independently RE-CHECKS _user_has_ig_creds and refuses (400) for any IG-linked account, so the guard holds even if the UI is bypassed; being on the admin-only /api/users route, only admins can reach it. GET /api/users now returns has_ig per account to drive the UI. On success the modal shows the password once so the admin can relay it out-of-band. 86 tests passed (3 new: works for a non-IG account and calls reset_password; blocked 400 for an IG-linked account with the password never touched; 403 for a non-admin caller). Claude. [Completed]
    * P-01 (user 2026-08-08, URGENT) "Set temp password" appeared next to an IG-linked account (Alex / eahind@yahoo.co.uk), and clicking the Support checkbox reverted to blank. -- Root cause for BOTH: the running Flask server was stale (browser had the new index.html but server.py/web_users.py were not reloaded), so GET /api/users returned no has_ig (button defaulted on for everyone) and the support toggle's persistence wasn't loaded (so it reverted). Verified current code is correct: _OWNER_LOGIN='Alex' and _user_has_ig_creds('Alex')=True (owner falls back to the stored global IG creds), and set_support persists to data/web_users.json (users are JSON-backed via web_users; web_store/Supabase is only used for activity logs + IG audit). Fix: (1) restart the server [operational, done by user]; (2) hardened the front end so the "Set temp password" button renders ONLY when has_ig===false (explicitly IG-free) — a missing/undefined has_ig now shows nothing instead of defaulting to the button. Server already independently refuses (400) a temp password for any IG-linked account, so no password was ever actually settable on Alex. 33 targeted tests green; JS syntax clean. Claude. [Completed]
    * P-12 (user 2026-08-08) Correct the User-onboarding guides for the deferred-email reality. -- Updated docs/_guides_content.js "Support — Adding Users" and "Operations — User Management": removed the misleading "a setup email is sent automatically" (email is deferred), documented the Support checkbox at creation, and documented the admin-only "Set temp password" onboarding path (no-IG accounts only). Rebuilt all 20 docs/guides/*.docx via _build_guides.js. NOTE: docs/_guides_content.js and docs/guides/ are gitignored, so these live on disk but are NOT in git. Claude. [Completed]
    * P-10 (user 2026-08-08) Configure outbound email so account setup/reset emails actually send -- DEFERRED at user's request 2026-08-08. Finding: no email provider is configured (encrypted app_secrets store holds 12 keys — IG, Slack, EIA/FRED/CRONJOB — but NONE for email; neither RESEND_API_KEY nor YAHOO_USER/YAHOO_APP_PASSWORD is set), so send_simple_email() logs "No email sender configured" and returns False. Consequence while deferred: approving/creating an account and "Forgot password?" generate but cannot deliver the setup code, so new users can't self-activate by email. Interim path in use: the admin-only "Set temp password" affordance (no-IG accounts only) added this session. To enable later: add a Yahoo app-password (YAHOO_USER + YAHOO_APP_PASSWORD) or a Resend key (RESEND_API_KEY, optional RESEND_FROM) via the admin Credentials UI / app_secrets — no code change needed. [Deferred]
    * P-06 (user 2026-08-08) Approve Carl's pending account request and grant access -- DEFERRED at user's request 2026-08-08. The pending request "Carl" (email c.d.mason@gmail.com) remains un-approved in User Management; the older "Carl Mason" account stays parked on the malformed address. User 2026-08-11: a colleague will handle this one directly (approve + hand over credentials), not Claude. Operational action for the user's colleague, not a code change. [Deferred — handled by a colleague, not tracked further here]
    * P-06 (user 2026-08-08) Deliberately revert the "Carl Mason" account email BACK to "c.d.mason@gmail,com". -- A new account request came in from the clean address c.d.mason@gmail.com; to free that address for the new account (and avoid two accounts sharing one email), the user asked to park the old "Carl Mason" account back on the malformed value. Reverted in data/web_users.json via atomic rewrite. This intentionally undoes the earlier P-06 email fix above — kept as a separate row so the history is honest, not a silent overwrite. The clean address c.d.mason@gmail.com is now held only by the pending request "Carl"; the user will grant that one access. Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Squeeze History Tab
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Terms & Conditions Tab
------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Fees Tab
------------------------------------------------------------------------------------------------------------------------------------------------

    * P-09 Fees - Add another previous month tab so we can see two previous months plus current month -- Claude 2026-08-07: added a third "Two months ago" period, leftmost of the 3 buttons (Two months ago / Last month / This month, so far). Server-side, /api/fees now computes prev_month the same way as last_month (same _period_ig/_period functions, same IG-vs-app-ledger reconciliation), covering the calendar month immediately before last_month — contiguous, no gap or overlap. The existing 365-day IG history lookback already covered it, so no widening needed. 55 tests passed (incl. 1 new: test_fees_returns_a_third_two_months_ago_period). Claude. [Completed]

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Configuration
------------------------------------------------------------------------------------------------------------------------------------------------

    --------------------------------------------------------------------------------------------------------------------------------------------
    USER
    --------------------------------------------------------------------------------------------------------------------------------------------

    --------------------------------------------------------------------------------------------------------------------------------------------
    DATA
    --------------------------------------------------------------------------------------------------------------------------------------------
    * P-06 Data - Import all X details for Alex from GitHub Secrets [Cancelled]
    * P-06 Data - Import all Yahoo details for Alex from GitHub Secrets [Cancelled]
    * P-11 Data - Import all from GitHub Secrets \ .env that are not in Supabase
      -- Claude 2026-08-08: Audited every `secrets.X` referenced across .github/workflows/*.yml (32 names)
      against migrate_secrets_to_supabase.py's _ENV_SECRETS list, seed-secrets.yml's env: injection block,
      and the local .env. Found 7 secrets that were live GitHub Secrets read by app code via os.environ
      (notify.py, run_hvf_report.py, social_monitor.py, instrument_dossier.py, intraday_signals.py,
      setup_cronjobs.py) but had NO path into Supabase — not in the migrate script, not in the seed
      workflow's env block, not in the admin Credentials UI: CRONJOB_API_KEY, SLACK_BOT_TOKEN, SLACK_ORDERS,
      SLACK_RW_HVF, SLACK_SIGNALS_CHANNEL_ID, SLACK_TWITTER, SLACK_TWITTER_CHANNEL_ID. Added all 7 to
      _ENV_SECRETS and seed-secrets.yml's env:/prerequisite-check blocks; added the 6 Slack ones to
      CRED_SECTIONS in hvf_web/server.py so they're also admin-editable and covered by
      import_credentials_from_env. GITLEAKS_LICENSE was the only other gap found — deliberately excluded
      (consumed directly by the gitleaks Action in trading-secret-scan.yml, never read by app code, so it
      has no reason to live in the encrypted store). No secret values were ever printed, logged, or
      committed — only names were compared. Added test_migrate_secrets.py (4 tests): one scans every
      workflow file for secrets.X references and fails the build if a new one appears with no import path
      (the regression guard for this exact gap), one checks seed-secrets.yml's env: block matches
      _ENV_SECRETS (GITHUB_TOKEN exempt — it's Actions' ephemeral per-run token, not a stable credential),
      one pins the 7 specific names added today, one confirms _sources() never migrates a bootstrap secret.
      All existing suites (test_server_auth.py, test_instruments.py, test_setup_cronjobs.py,
      test_performance.py) still pass. [Completed]

    --------------------------------------------------------------------------------------------------------------------------------------------
    TRADING (SQUEEZE)
    --------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------
Application Focus - Security
------------------------------------------------------------------------------------------------------------------------------------------------

    --------------------------------------------------------------------------------------------------------------------------------------------
    OBFUSCATION
    --------------------------------------------------------------------------------------------------------------------------------------------

    --------------------------------------------------------------------------------------------------------------------------------------------
    RECOMMENDATIONS
    --------------------------------------------------------------------------------------------------------------------------------------------

Web App: Mobile devices

    --------------------------------------------------------------------------------------------------------------------------------------------
    ENHANCEMENTS - GENERAL (mobile devices)
    --------------------------------------------------------------------------------------------------------------------------------------------

    --------------------------------------------------------------------------------------------------------------------------------------------
    BUGS - GENERAL (Mini iPad)
    --------------------------------------------------------------------------------------------------------------------------------------------

    --------------------------------------------------------------------------------------------------------------------------------------------
    BUGS - GENERAL (mobile devices)
    --------------------------------------------------------------------------------------------------------------------------------------------

===============================================================================================================================================================
= X (Twitter)
===============================================================================================================================================================
