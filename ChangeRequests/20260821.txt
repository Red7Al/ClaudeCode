HVF Web Application — Change Requests

## Testing and quality

* P-01 BUG Restore the Offline Python Regression Tests gate after its 2026-08-22 CI failure; retain the enriched Instruments RVOL provenance/status contract and verify a fresh GitHub Actions run before release -- fixed stale exact-response assertion; GitHub Actions run 32586311609 passed all 248 selected tests on 2026-08-22. [Completed]

1. Time: 09:00:00
   Request: Reconcile every Back Test summary figure with its funded transaction-evidence rows.
   Result: Completed and regression-tested. The Back Test summary now reconciles its funded/ skipped counts, net gain and ending wallet to the displayed transaction-evidence rows; any mismatch withholds the headline return and shows the evidence discrepancy.
   Status: complete

2. Time: 09:30:00
   Request: Set the Replay model defaults to a 5% maximum position size and 20 maximum open positions.
   Result: Completed and publicly verified: Replay defaults are 5% and 20. Saved user configuration is unchanged.
   Status: complete

3. Time: 10:15:00
   Request: Following a full review, make Let winners run simulation-only until per-user account routing and a safe live handover are implemented and verified.
   Result: In progress. The live path is fail-closed; the historical report remains available. The legacy Owner trading-profile UUID is explicitly mapped to Alex without rewriting stored positions, and the two-hour bridge now calls the guarded manager even when no new candidate is found. The 2026-08-22 simulation review passed 68 relevant regression tests and a live historical replay: 3,212 resolved eligible trades, zero target-lock breaches, exact exit/capacity reconciliation, and a valid improved verdict. On the £10,000 / 5% / 20-position model, 1%–4% run trails all improved versus sell-at-target; 4% was strongest in that bounded range (+£387.10). This is daily-bar historical evidence, appropriate to a multi-day hold model but not an intraday execution claim. Prepared for decision on 2026-08-23: docs/LET_WINNERS_RUN_DECISION.md sets out what the feature does, the measured historical evidence, the guards already in place, what remains unverified, and the one risk that matters -- removing the IG take-profit makes the gain depend on the manager continuing to run, and the Order Bridge runs only every two hours on weekdays, so positions are unmanaged overnight and at weekends with nothing detecting it. An observe-only mode was added (LWR_OBSERVE_ONLY, shipped off) which runs the COMPLETE decision path against the real account and records what it would have done without calling any mutating IG endpoint; six tests assert it stays silent even when a position qualifies. That converts the live path from untested to evidenced without taking the risk first.
   Status: in progress

4. Time: 13:45:00
   Request: Restore meaningful GitHub Offline Python Regression Test status after stale checks caused every recent workflow run to fail.
   Result: Completed and verified by GitHub Actions run 32483421114: the full offline suite passed after deployment-only IONOS secrets were excluded and the approved 20-position default was reflected in the regression test.
   Status: complete

5. Time: 14:10:00
   Request: Replace the Best over 3 years card's finalist shortlist with a full, auditable configuration-grid review and repeat the validation daily.
   Result: Completed and end-to-end verified. The card replays every generated, enforceable three-year configuration rather than reducing candidates to 40 and then 12. GitHub Actions run 32525904248 completed successfully on commit b200155: the refreshed workflow evaluated 39,000 annual and 68,226 three-year configurations over 10,006 rows, persisted a complete audit with no missing trigger features, and correctly published no supported three-year recommendation (the leading three-year configuration funded 90 trades, below the 125-trade evidence rule). Source-confirmed zero-volume trigger bars are recorded as N/A for volume-derived filters rather than fabricated.
   Status: complete

6. Time: 14:25:00
   Request: Make Best Settings transaction evidence reconcile visibly with its return card by adding a Net gain total at the bottom of the table.
   Result: Completed and live-verified. The funded proof rows now show a Net gain total; cross-filters show an explicitly labelled visible subtotal and retain the all-funded total for comparison.
   Status: complete

7. Time: 14:30:00
   Request: Remove the unused Closed date/time column from the IG Account transaction table; closed transactions are hidden by default.
   Result: Withdrawn by the requester before release. The Closed date/time column remains.
   Status: cancelled

8. Time: 14:35:00
   Request: Populate the required Now column when IG Account uses Aggregate rows.
   Result: Completed and live-verified. Aggregate rows show a size-weighted current price from their open-position components.
   Status: complete

9. Time: 14:40:00
   Request: Allow a user to select their own open IG positions and close them only after an explicit confirmation.
   Result: Completed and live-verified. Users select their own open positions and confirm the named market-close request; the server re-reads that user's live account and rejects any ID that is no longer open. No position was closed during release verification.
   Status: complete

10. Time: 14:45:00
    Request: Allow every data-table column to be sorted, including Transaction evidence in Performance.
    Result: Completed and live-verified. Existing purpose-built sorting remains authoritative; the shared sorter covers headings without a specialised handler, including dynamic transaction-evidence tables, and correctly treats displayed currency, percentages, Unicode-negative values and multipliers as numbers. The live site served the deployed sorter and its status endpoint passed.
    Status: complete

11. Time: 14:50:00
   Request: Treat every missing trigger-time RVOL, VWAP direction, or ATR direction as a data-quality defect; audit and fix it rather than presenting a silent blank.
   Result: Completed and verified by the persisted full-grid audit. It measures trigger-time RVOL, VolumeScore, VWAP and ATR before issuing any recommendation; the 2026-08-22 audit completed with no missing trigger features. Source-confirmed zero-volume observations are explicit N/A for volume-derived filters, never silent blanks or fabricated values.
   Status: complete

12. Time: 14:55:00
    Request: Show an explicit loading state while Best Settings calculates transaction evidence for a newly selected card.
    Result: Completed and live-verified. Selecting a card immediately identifies the configuration whose chronological funding evidence is being replayed, confirms that it does not fetch data, change settings or place orders, and preserves a newer selection if an earlier calculation finishes later. It also provides a retryable error state. The deployed page was verified to contain the loading state and the live API status check passed.
    Status: complete

13. Time: 15:00:00
   Request: At iPad mini width and above, do not allow Best Settings cards to occupy more than three rows; remove the lowest-return displayed recommendation if necessary.
   Result: Completed and live-verified. Phone screens show at most six cards; iPad-mini range may show nine; laptop screens show up to eight only when they fit in two rows, otherwise the lowest-return cards are removed until they do. The Best over 3 years evidence card is protected from trimming and remains visible with its return and trade-sample context.
   Status: complete

14. Time: 15:10:00
    Request: Clarify the replay model's gross exposure, reserved margin, free margin and stressed-loss illustration without changing sizing, returns or orders.
    Result: Completed and live-verified. The replay panel distinguishes one position's notional from broker margin, shows gross exposure at the selected position cap, and quantifies a 4% move for one position and the whole book. It explicitly states that illustrative 10x leverage reduces required margin only; it does not multiply replay returns. Static live content was verified after deployment.
    Status: complete

15. Time: 16:12:00
    Request: Make confirmed IG position closes auditable and unambiguous: retain each broker outcome, show it in the GUI, and never leave a user unsure whether a selected position closed.
   Result: Built, tested and deployed, but BLOCKED on the same dependency it was deferred for. The close path already recorded every attempt durably: _append_ig_close_audit has written a host-side, append-only trail since 2026-08-21, deliberately independent of Supabase so it survives the outage that might have caused the problem. Nothing ever read it back, so the evidence existed only on disk and a user who dismissed the result dialog or reloaded had no way to see what IG actually said. Added /api/ig-close-audit and a Close history reveal in IG Account, lazily loaded like Closed trades and opened automatically once a close completes, showing when, which position, the outcome in plain words and what IG reported. A user sees only their own attempts and an admin sees everyone's, which is what makes it an audit; that boundary is tested explicitly, as is a torn final line not hiding the rest of the trail. Nine new tests, 363 passing. No change to the close path itself and no position was closed at any point. STILL BLOCKED: the shared-host Python worker is serving a previously-loaded module, so the new route returns 404 over HTTP while the older /api/ig-closed still answers 401. Verified rather than assumed -- the deployed hvf_web/server.py on the host contains the route, and the host's own interpreter registers /api/ig-close-audit in the Flask url_map, so the code is correct and current where it sits. Ruled out: response caching (cache-busted), a second deployed copy (only /home/www/squeezescanner carries it; the rest are August backups), and stale bytecode (__pycache__ cleared, server.py and cgi-bin/app.py both touched, which is the documented reload signal). The web tier runs in a sandbox the SSH session cannot see: ps shows only the session's own processes and there are no FastCGI sockets, so the worker cannot be recycled from here. It needs a restart from the IONOS control panel, or the worker's own recycle, which earlier changes today did eventually get. Re-check with: curl -o /dev/null -w '%{http_code}' https://www.squeezescanner.cloud/api/ig-close-audit -- 401 means it is live, 404 means still stale. UNBLOCKED and verified: the worker recycled and /api/ig-close-audit now answers 401 to an unauthenticated request where it returned 404 before, so the route is live. The admin-gated GUI view itself is the requester's to eyeball.
   Status: complete

16. Time: 19:00:00
    Request: Show an explicit loading message in Instruments while its data is loading.
   Result: Completed and live-verified. The tab retains a Loading instruments state in both its count and table until the shared records request completes, rather than showing a misleading empty result. The deployed page contains the state and the live data status check passed.
   Status: complete

17. Time: 19:15:00
   Request: Resolve missing current RVOL values in Instruments, including III, ADM, ALW and AAL.
   Result: Completed. III and ALW use current reported-volume bars; ADM and AAL use the newest usable reported-volume bar when the latest close has no volume. The Instruments response labels the metric date rather than rendering a blank.
   Status: complete

18. Time: 19:30:00
   Request: Review every current Instrument row; where expected RVOL, VWAP, ATR or VolumeScore data is missing, determine the cause and resolve it.
   Result: Completed and verified. The refreshed 2026-08-22 audit assessed all 1,421 rows: 1,384 are complete (including 19 explicitly-labelled NSE fallbacks for BSE instruments), 34 have no meaningful reported volume and are N/A, two delisted equities are explicitly labelled, and there are zero missing VWAP values, zero missing ATR values and zero unresolved price-history rows. The final VWAP gap was QUB.AX: its repeated $5.11, zero-volume bars after 8 July are the post-suspension scheme price, not live observations, so it is classified as delisted rather than assigned a fabricated VWAP.
   Status: complete

19. Time: 09:10:00
    Request: Resolve the web page becoming unresponsive while Best Settings loads.
   Result: Completed and user-verified. Annual Best Settings data now renders before the expensive three-year evidence request begins, avoiding the previous simultaneous heavy requests. The user confirmed the live site is no longer raising errors.
   Status: complete

20. Time: 10:00:00
    Request: Determine a safe per-user IG routing design for Let Winners Run.
    Result: In progress. The opt-in switch remains per-user and globally disabled. Additive owner and hashed-account fields are wired to new web-placed working orders and positions without rewriting legacy data; the scheduled manager groups work by owner and re-enters that owner's session immediately before every trailing-stop mutation, failing closed where an approved binding is absent. Owner-scoped live scheduler verification remains outstanding; no broker action is authorised during this release-readiness pass. See docs/LET_WINNERS_RUN_DECISION.md (2026-08-23) for the owner-scoping already implemented and verified by test, and for the three decisions still outstanding.
    Status: in progress

21. Time: 10:30:00
    Request: Add a Risk Awareness tab immediately to the right of How it works.
    Result: Completed and live-verified. The public tab provides educational risk-profile prompts and a concise check of loss tolerance, concentration, correlation and leverage/margin. It does not alter trading configuration or submit orders. A missing tab-registry entry initially caused a blank panel; this was corrected and the live public page was verified to contain both the risk tab registration and the Risk Awareness view. The live API status also passed.
    Status: complete

22. Time: 13:45:00
    Request: Always show a clear loading message while data is being retrieved, including Scanner Report.
    Result: Completed and live-verified. Scanner Report now has loading states for its count and table, and an explicit retry message if its initial records request fails. Stale public copy was also updated from 1,300+ to 1,400+ instruments. The deployed public page contains the Scanner loading state and the new audited wording.
    Status: complete

23. Time: 18:10:00
    Request: Add all current S&P 500 constituents without removing instruments already covered; replace obsolete How it works pre-order language with order creation; add visual risk-profile curves.
    Result: Completed and live-verified. Added the 402 missing constituents from the 2026-08-22 current 503-share-class public constituent list, preserving two existing legacy tickers. Scanner Snapshot Publish run 32584330544 succeeded; the live API reports 1,773 instruments generated at 2026-08-22T16:30:48Z. How it works now describes review and IG order creation rather than pre-orders, and Risk Awareness has accessible concave, linear and convex profile charts. The 2026-08-22 deployment verified those public views and the updated over-1,700-instruments copy.
    Status: complete

24. Time: 18:25:00
    Request: After the active work is verified, update Change Requests, GitHub, and all relevant documentation/Markdown before handover; prepare for a full review of code and documents.
    Result: In progress. The S&P rebuild, expanded-universe audit, public UI, regression gate, GitHub history and relevant documentation are reconciled. This gate remains open only for Let Winners Run owner-scoped broker handover: it must be live-verified while still fail-closed before it can be marked complete.
    Status: in progress

25. Time: 19:30:00
    Request: P-01 BUG Make the Change Requests tab read this register. The numbered "Time/Request/Result/Status" block format used by 20260820.txt, 20260821.txt, 20260820-COMPLETE.txt and 20260813-COMPLETE.txt is invisible to the parser, which only accepts lines beginning with "*": 33 of the 34 recorded items in those four files do not appear in the tab at all.
    Result: Completed and deployed in 00c5579. hvf_web/server.py now parses the numbered block format alongside the "*" format. Verified against all 29 real register files: every previously-parsed row is byte-identical and 50 previously-invisible rows are recovered (20260821 1 -> 26, 20260820 0 -> 9, 20260820-COMPLETE 0 -> 6, 20260813-COMPLETE 0 -> 10). Status words map correctly, including "deferred external dependency" and "out of scope". No register was reformatted. 15 new tests, one of which reconstructs the original bug. The admin tab's own render is gated behind an admin login this session does not hold, so that last visual check is the requester's.
    Status: complete

26. Time: 19:45:00
    Request: P-01 BUG "Apply this configuration" must not be enabled in Best Settings when nobody is logged in.
    Result: Deployed in 00c5579, live render not yet re-confirmed. The card renderer emits the button only when AUTH is set and offers "Log in to apply this configuration" otherwise; applyConfigFromReport also fails closed for any other caller. Both are covered by executing tests. The post-deployment logged-out check could not be repeated: both site origins now hold a session, and the in-memory logged-out re-render froze on the separate unresponsiveness defect recorded at item 29. To be confirmed on a genuinely logged-out browser. Live confirmed working by the requester on 2026-08-23 ("change requests looks better").
    Status: in progress

27. Time: 19:50:00
    Request: P-01 BUG The "Last 12 months / From / To" date window should not appear on Best Settings; it was expected to have been removed.
    Result: Completed, deployed in 00c5579 and live-verified. The requester's recollection was correct and the intent was already in the code: pfPanel() adds the "hidden" class to #pf-subnav for the Best settings and Let winners run sub-tabs. The class was inert because #pf-subnav carried an INLINE display:flex, which outranks .hidden{display:none} -- the same inline-style defeat already recorded against the dead "Filters" button. Layout moved into CSS with a matching #pf-subnav.hidden rule. Measured on the live Best settings view before and after: the date preset and From/To inputs rendered visible before, and compute to display:none after.
    Status: complete

28. Time: 20:05:00
    Request: P-06 Sort the tabs in the Tab Visibility section into the order they appear on screen; the present order is confusing.
    Result: Deployed in 00c5579, live render not yet confirmed. renderTabVis() grouped chips as Settings, Instrument Operations, Application Operations and Other, an order unrelated to the tab bar. It now renders one flat list in navigation order, taken from the #tab-* buttons in markup order with the Settings and Operations groups expanded in place. A tab present in TABS but absent from that order still gets a chip, at the end, because a missing chip is never submitted by saveTabVis() and would strand that tab's visibility. Covered by executing tests. The panel is admin-gated, so the visual check is the requester's. Superseded and completed by item 37: the groupings were restored on 2026-08-23 with the chips inside each sorted into tab-bar order, which is what this request asked for.
    Status: complete

29. Time: 20:10:00
    Request: P-01 BUG "This page isn't responding" is appearing more and more, for example when clicking IG Account; filtering on Scheduled Jobs is also very slow.
    Result: Completed, deployed in d7faa6a and confirmed by the requester on 2026-08-22 ("freeze is gone"). Cause: paintOrdersPerf built EVERY ledger entry into one synchronous innerHTML. Against the live three-year payload (11,682 rows in, 11,669 ledger entries out) that is 7.5 MB of HTML and roughly 233,000 DOM elements, parsed on the main thread -- which is Chrome's "This page isn't responding" dialog, and it blocks every other tab because the main thread is shared. Measured rather than assumed: _winLedger was the first suspect and is NOT the bottleneck at 198 ms per pass (1.4 s for all seven passes of a paint), and building the HTML string costs only about 16 ms, so the DOM parse and layout were the entire cost. Item 19 deferred the three-year FETCH but left this render blocking, so the freeze moved rather than went away. Fix: the table now materialises the first 1,500 ledger rows in the same chronological order -- 1.0 MB and about 30,000 elements, an 87% reduction -- with a footer stating the true total and a "Show all rows" control. NO FIGURE CHANGES: the ledger is still computed over every row and the trade count, wallet and net gain are read from it, never from the table, which a test asserts. The user-visible difference is that the evidence table shows 1,500 rows instead of 11,669 until "Show all rows" is pressed. Six new tests, one reconstructing the unbounded render. The live paint could not be observed from here, because /api/winners takes 34.9 s and /api/winners?years=3 takes 16.9 s and the shared host serialises them, so the data never arrived inside a measurement window; the requester confirmed the result directly instead. That API latency remains and is recorded separately as item 32.
    Status: complete

30. Time: 20:15:00
    Request: P-01 BUG Version history is not being maintained.
    Result: Deployed in 00c5579, live render not yet confirmed. Cause: server._version_entries() builds the list from `git log`, falling back to a Supabase store and then to hvf_web/data/version_history.json. On IONOS the git call ALWAYS fails, because the package deliberately excludes .git; the Supabase copy was seeded once by migrate_runtime_state_to_supabase.py and never refreshed; and the file was never shipped at all, since hvf_web/data is excluded from the package. All three sources were therefore frozen. build_ionos_package.py now generates the file from git at package time and adds it to the archive explicitly; a checkout without git history logs a warning and leaves the host's previous copy rather than failing the build. Verified in the built archive: 1,082 entries, newest 2026-08-22 1984e55. The tab is admin-gated, so the visual check is the requester's. Superseded and completed by item 36: the first attempt shipped the generated file to a path the server does not read, and the stale Supabase seed shadowed it in any case. Both were fixed on 2026-08-23 and the deployed archive now carries the current history where _version_entries reads it.
    Status: complete

32. Time: 22:40:00
    Request: P-01 BUG The winners endpoints are slow enough to dominate the Best Settings wait now the render freeze is fixed.
    Result: In progress -- the repeat-load cost is fixed and measured; the cold build remains. Cause found: both trigger-feature caches were single-slot carrying a "years" tag, so a hit required the tag to match and a call for a different window EVICTED the previous one instead of sitting beside it. Best Settings requests the annual window and then the deferred three-year window -- the two calls the page always makes together -- so neither was ever served warm, and _volscore_trigger_feature_map had no cache at all despite being called on every request alongside its sibling. That also explains the odd original reading, the longer window measuring FASTER: the shared _sqa_all_rows population happened to be warm from the call before it. Both caches are now keyed per window (bounded at four entries, since callers clamp years to 1-4) and the feature map has its own. Values and TTL are unchanged and each window still builds its own features, which is what keeps the three-year review from borrowing the annual map and showing blank VWAP/ATR cells; all callers are read-only. Measured live after deployment, in the order the page issues them: annual 32.8 s cold then 0.83 s warm, three-year 34.0 s cold then 1.28 s warm -- previously the second annual call would have been cold again because the three-year call had evicted it. Repeat loads are therefore about 25-40x faster. Remaining: the cold first build is still around 33 s and is unaffected by caching; serving a precomputed result on a schedule, as the Best Settings audit already does, is the next step. Note the measurements were taken while snapshot rebuild 32598244962 was scanning, so the cold figures may be pessimistic. Closed by item 38: the cold build is now served from a precomputed payload refreshed at the end of every snapshot publish. Measured on 2026-08-23 with no cold outlier recurring: annual 0.49 s, three-year 1.00 s, against 34.9 s and 16.9 s before any of this work.
    Status: complete

33. Time: 23:10:00
    Request: P-20 Repackage the skills and bring the artifact index up to date; the shipped .skill archives had drifted from skills_src.
    Result: Completed. All 14 packages were stale or missing on 2026-08-22: the five that existed were roughly two months behind their sources (packaged 12-20 June against sources changed 4 August onward, ah-web-formatting changed that day), ah-x-publications had grown 6.2 KB to 15.9 KB in source, and six skills had never been packaged. Repacking was verified non-destructive first -- no file existed in any archive that was missing from skills_src. Added build_skills.py (with a --check mode) so packaging is repeatable and deterministic, and test_skill_packages.py so the drift becomes visible instead of silent. Because .gitignore treats *.skill as a build artifact, a missing archive skips rather than failing on a clean checkout, while an archive that exists and disagrees with its source fails; verified in a detached worktree that the guard genuinely fails on the five stale committed archives and skips the nine unbuilt ones. docs/PROJECT_ARTIFACTS.md was also stale and now lists all 14 skills, all 19 memories and the real test suite. 301 tests pass.
    Status: complete

34. Time: 21:05:00
    Request: P-01 BUG Resolve the analysable-data gap behind item 23: the 402 newly added S&P 500 constituents were listed but largely unanalysed.
    Result: Completed and live-verified. Item 23 was closed on the instrument COUNT (1,773 live) but the coverage behind it was not there: 345 of the 402 new names had no 52-week range (14.2% coverage against 100% for the S&P names already present) and 288 had no sector (28.4% against 98.1%). Two distinct causes, both "a backfill exists but nothing ran it". First, 52-week high/low is read from price_history, and the daily Price Data Refresh audits only a 30-day lookback, so a brand-new ticker could never accumulate a year: ran the documented one-off deep backfill (1,100 days) in three batches over the 345 names -- runs 32597007024, 32597321755 and 32597859813 all succeeded, writing 257,968 bars in about nine minutes total. Second, sector comes from sector_cache, whose backfill shipped 2026-07-17 with a CLI that nothing ever scheduled; ran it (run 32597922913): 438 newly resolved, 1,566 of 1,773 tickers now carry a sector, the remainder being FX/futures/indices that legitimately have none. Added trading-sector-backfill.yml and registered it at 06:00 Sunday in setup_cronjobs.JOBS so the cache follows the universe from now on. The live API still shows the old figures because sector is baked in at snapshot-build time and the 52-week map is cached against the snapshot's timestamp; Scanner Snapshot Publish run 32598244962 completed and the coverage was re-measured against the new 2026-08-22T21:37:45Z snapshot: the 402 new names now show 100% 52-week coverage (was 14.2%, 345 missing -> 0) and 99.5% sector (was 28.4%, 288 missing -> 2). Universe-wide, 52-week coverage is 1,772 of 1,773 and sector 1,566 of 1,773. The remainder is legitimate rather than missing: the one instrument without a 52-week range is delisted ANSS, and the rows without a sector are FX, government bonds, commodities, indices, crypto and investment trusts, none of which carry a GICS sector.
    Status: complete

35. Time: 22:20:00
    Request: P-01 BUG A failing schema statement on the order-placement path could leave a live IG order with no working_orders record.
    Result: Completed and regression-tested; awaiting the schema run and deployment. _log_working_order_to_db named the two Let Winners Run columns in EVERY insert, and those columns existed only because ig_shim created them at runtime with an ALTER TABLE on the order-placement path -- they were declared in no schema file, so a database rebuilt from supabase_schema.sql or run_schema.py did not have them. The whole function body is a documented "never raises" handler that only logs, so an ALTER failing for any reason (missing privilege, lock timeout, a fresh database) silently skipped the INSERT and produced a live IG order with no record of it. _log_position_to_db already guarded this correctly and the order path now matches it: the DDL runs only when there is an owner binding to write, and the base insert no longer references those columns at all. Both columns are also declared properly in run_schema.py for working_orders and positions, so the runtime DDL is a redundant safety net rather than the only thing standing between a placed order and its record. Six new tests, one reconstructing the lost row. 312 tests pass, including the full trailing-stop and order-bridge suites.
    Status: complete

36. Time: 08:55:00
    Request: P-01 BUG Version history still shows 18/8 as its latest entry.
    Result: Completed and deployed. Two faults, one of them introduced by the earlier attempt at item 31. First, _version_entries consulted the Supabase copy and read the file fallback only "if not source"; the Supabase copy is a one-off seed that migrate_runtime_state_to_supabase.py wrote once and nothing has updated since. Confirmed directly against web_json_store: 958 entries, newest 2026-08-18, updated 2026-08-18 -- exactly the date reported. Once that seed existed the file could never be reached. The two fallbacks are now compared on their newest entry so the fresher wins either way. Second, the earlier packaging change wrote the generated file to hvf_web/data/version_history.json, but server._VERSION_FILE is the REPO-ROOT data/version_history.json, so it shipped where nothing reads it and that fix achieved nothing; corrected, and the archive now carries 1,095 entries at the exact path with newest 2026-08-23. The deploy script's protected-path guard correctly refused the first attempt, since it blocks everything under data/; it now carries one documented exception for this generated release manifest, which is not host runtime state. An existing test was reading the real repo data file as an ambient input and passed only because that file was never reached; it now supplies the fallback explicitly. 318 tests pass.
    Status: complete

37. Time: 09:05:00
    Request: P-06 Restore the Tab Visibility groupings and order the tabs within each grouping.
    Result: Completed and deployed. The first pass at item 29 removed the headings altogether, which was an over-correction: only the ORDER inside the groups was ever arbitrary. Settings, Instrument Operations, Application Operations and Other are restored, with the chips inside each sorted into tab-bar order.
    Status: complete

38. Time: 09:40:00
    Request: P-10 Remove the cold-build wait on the winners endpoints by precomputing them.
    Result: Completed and deployed. Building one window costs about 33 seconds and Best Settings requests two on every visit, so keying the caches by window fixed repeat loads but a cold worker still paid in full. run_winners_precompute.py now builds both windows after the daily refresh and stores them in web_json_store, the same mechanism run_best_settings_audit.py already uses. The payload comes from server._winners_payload, the single definition the endpoint itself calls, so a stored copy cannot drift into a lookalike build of a different population. A stored payload is served ONLY when the snapshot generation time recorded alongside it matches the one now live and the copy is under a day old, and the script refuses to store an empty population, so a missed, failed or stale precompute makes the page slow rather than wrong. Registered as Winners Precompute at 05:50 UTC Mon-Sat, after the 04:30 price refresh and 05:30 report. Ten new tests, including one asserting the stored payload equals a live build of the same window. Measured live rather than assumed, which caught a silent failure: the first run stored 3,782 and 11,672 rows with an EMPTY dataset key, because a GitHub runner has no built snapshot, and the server correctly rejected both while the job reported success. Confirmed against web_json_store directly; the annual endpoint's apparent 0.54 s at that point was a warm in-process cache, not the store. The key now comes from the local snapshot where one exists and otherwise from the live /api/status, and a missing key is a reported failure that stores nothing. The warm-up also runs at the end of the snapshot publish, where the runner still holds the snapshot it just published, because ANY new snapshot invalidates the stored copies and a once-daily job would only have helped until the next refresh; it is non-fatal there, so a failed warm-up leaves the site building live. After the fix the three-year window served in 1.05 s cold against 32.5 s before, repeatably. One annual request in the same window still took 34.2 s before settling at 0.57 s: a scan was mid-flight, and _load_snapshot carries a short verification cache, so workers can transiently disagree about the current snapshot and fall back to the live build. That is the intended fail-safe direction, and the publish-time warm-up is what makes it settle; worth re-checking once no scan is running. Confirmed on a fresh dataset, which removes the earlier ambiguity: after the 2026-08-23T10:04Z snapshot published, the endpoints measured 33.2 s and 21.1 s because the new snapshot correctly invalidated the stored payloads; re-running the precompute against that snapshot took them to 1.5 s and 2.1 s, repeatably. A warm in-process cache cannot explain that, because the dataset had just changed, so this isolates the store as the cause. The publish-time warm-up did not fire for that scan only because the run had been dispatched before the step existed.
    Status: complete

39. Time: 09:50:00
    Request: P-06 The "Saving" message on Apply this configuration should be clear to the user, for example in amber.
    Result: Completed and deployed. The button now has four distinct states: amber while the save is in flight, green when applied, red and clickable again on failure, then back to its normal appearance. The amber is a new --warn theme token carrying the #d29922 this file already used in about 97 places, with a darker value in the light theme where #d29922 fails contrast on a white panel. The button also carries aria-busy while saving.
    Status: complete

40. Time: 09:55:00
    Request: P-06 Show "Data loading" wherever a slow data set is loading, for example Batch Activity and System Logs.
    Result: Completed and deployed. Scheduled Jobs and Change Requests already did this by hand; Batch Activity, System Logs, Version History and My Activity showed an empty table until their fetch returned, which during a slow admin query is indistinguishable from "there is nothing here". All four now use one shared _rowsLoading helper that reads the column span from the table's own header rather than hardcoding it.
    Status: complete

41. Time: 10:00:00
    Request: P-01 BUG iPad mini should show nine Best Settings cards, not eight.
    Result: Completed and deployed. An iPad mini is 768 wide in portrait but 1024 in landscape, and the tablet band ended at 850, so the landscape orientation fell into the laptop band and showed eight. The band now runs to 1024. The card count and the post-layout row cap were also separate literals, which is how the two halves of one rule could disagree; they now share a single BEST_TABLET_MAX constant, and a test asserts nine cards can actually fit the rows they are given. Verified by execution across seven viewport widths, including a reconstruction of the old boundary showing eight at 1024.
    Status: complete

42. Time: 10:20:00
    Request: P-06 "Data loading... Scanner Report" is too much information; the page title above already says what is loading. Check every Data loading message and make them consistent.
    Result: Completed and deployed. Twenty messages named their own data set -- Scanner Report, run stats, closed trades from IG history, 12-month replay, change-request files and so on -- duplicating the heading directly above them and differing in wording each time; a second phrasing, "Loading instruments", existed too. All now read exactly "Data loading". The two dynamic suffixes are deliberately retained because they are live status rather than a description: the numeric progress the requester previously asked to keep (for example 230/1431) and the "retry n of 3" counter, which is what distinguishes still-working from stuck. 52 identical messages remain, with those two exceptions, and a test fails on any new message that names its data set.
    Status: complete

43. Time: 11:30:00
    Request: P-01 BUG A deploy can update every file while the API keeps serving a previously-loaded module, with no signal that it happened.
    Result: Completed and proven on its first real use. IONOS shared hosting keeps the imported Flask module resident behind the CGI wrapper and offers no way to restart it: there is no control panel button, and the SSH session is a sandbox that can see neither the web worker nor any FastCGI socket. It happened twice on 2026-08-22/23, and was noticed only because a new route returned 404 -- a change adding no route gives no signal at all, so the version-history and winners-store fixes shipped the same day could not be verified. /api/build now reports what the running PROCESS loaded, read once at import: reading it per request would report the file on disk, and the disk is not what is stale, so a stale worker would report itself as current. The packager stamps each archive with a short fingerprint of the commit rather than the commit itself, since the endpoint is unauthenticated so the deploy can check it before anyone logs in. The deploy compares the two and states plainly when the API is not running what was just shipped, as a warning rather than a failure because the static release is genuinely live and no restart exists to offer. The first deploy carrying it reported "API worker is current", and the previously 404ing close-audit route began answering, which also unblocked item 15.
    Status: complete

44. Time: 12:10:00
    Request: P-02 Sweep the repository for other cases of "the mechanism exists but nothing invokes it", after five were found by accident in one day.
    Result: Completed. Checked all 52 runnable scripts against every workflow, the cron registry, shell scripts, documentation and cross-imports. Seven are invoked by nothing. Four are correctly manual and need no action: recompute_squeeze_outcomes is documented as a one-off migration, generate_x_cards is an on-demand fallback for when Slack chart upload is unavailable, analyze_volume_score_windows is research, and run_price_history_prune is deliberately unscheduled -- its own header says the VACUUM FULL step takes an ACCESS EXCLUSIVE lock and must never run on a schedule, and the delete is irreversible from inside the system. Measured rather than assumed: only 5,289 rows, 0.3%, are older than the 4.5-year retention, oldest bar 2022-02-17, so there is nothing meaningful for it to reclaim anyway. One is a genuine gap and is recorded separately as item 45. Market cap was also checked, since mcap_backfill is unscheduled: coverage is 96.1% and the 50 without one are futures, indices and a few foreign listings that legitimately have none, so no action.
    Status: complete

45. Time: 12:15:00
    Request: P-01 BUG The daily Scanner Report email was built on request but has never run.
    Result: Not started -- awaiting authorisation, because acting on it sends email to every account holder. run_scanner_report_email.py was written for ChangeRequest P-07 on 2026-08-07 ("Email the scanner report each day to the account holder"), emails each enabled web user their own Scanner Report filtered by their saved display floors and personal trading filters, and has its own regression suite in test_scanner_report_email.py. No workflow, cron entry, shell script or other module invokes it, so it has never executed and no account holder has ever received one. The fix is a workflow plus a setup_cronjobs.JOBS entry, which is mechanical; it is deliberately not done unilaterally because enabling it starts sending mail to real users.
    Status: not started

46. Time: 12:20:00
    Request: P-02 The Supabase database is close to its size allowance.
    Result: Not started -- reported for a decision, no data touched. Measured 2026-08-23: the database is 461 MB against the 500 MB free-tier allowance documented in run_price_history_prune.py, so about 92% used. price_history is 339 MB over 1,835,580 rows and is already at its 4.5-year retention, so pruning it would reclaim almost nothing. signal_log is 81 MB over 216,495 rows spanning 2026-06-01 to 2026-08-06 only; it stopped growing when the session monitors were disabled, so it is static dead weight rather than an accelerating cost. Note the today's price backfill for the 402 new S&P constituents added roughly 258,000 rows, of the order of 48 MB, which is a material part of the current figure. Options, all of which delete data or cost money and none of which should be taken without an explicit decision: shorten the price_history retention below 4.5 years, prune the now-static signal_log, or move off the free tier. Worth noting the deferred Supabase Storage 402 is a separate quota from this one. Actioned on the requester's instruction to prune signal_log by 75%, after confirming the exact cut. The obvious rule -- delete the oldest 75% by date -- would have removed the most recent row for 12 of the 61 tickers, and all four consumers (hot_stock_tweet, instrument_dossier, the X-draft enrichment and the session watchdog) read `distinct on (ticker)`, so those instruments would have silently returned nothing. The requester chose the variant that keeps each ticker's latest row, which costs 12 rows. Deleted 162,359 rows of 216,495 (74.99%), leaving 54,136. signal_log went 81 MB to 19 MB and the database 461 MB to 399 MB after VACUUM FULL, so about 80% of the 500 MB allowance rather than 92%. Verified by running the consumers' own distinct-on query: it still resolves all 61 tickers, exactly as before. The DELETE alone moved neither figure, as its own price-prune counterpart documents; the space came entirely from the VACUUM.
    Status: complete

47. Time: 13:40:00
    Request: P-20 Housekeeping, documentation and research pass.
    Result: Completed. Housekeeping: checked and found essentially clean. The five root trigger/output files that look like dead weight -- query_output.txt, query_trigger.txt, slack_test_trigger.txt, spotlight_trigger.txt and us_open_trigger.txt -- are each still referenced by a live workflow, so they were deliberately left alone rather than tidied away. What remains is one untracked Codex session pointer and some gitignored working directories, none of which are worth deleting without asking. Documentation: PROJECT_ARTIFACTS.md has listed eight candidate artifacts since 2026-07-24 and none existed. Wrote the highest-value one, docs/OPS_RUNBOOK.md, because a day of operational discovery was otherwise recorded only in commit messages and this register: health checks, deploying and the resident-worker trap, failed scheduled jobs, the scanner scan, Best Settings latency, the price and sector backfills, database size and the delete-versus-vacuum distinction, and the Resend verified-domain constraint. Every command in it was run and every figure measured on 2026-08-23. The artifact index is updated to mark it delivered.
    Status: complete

48. Time: 14:10:00
    Request: P-10 Show win:loss ratios on the Best Settings cards.
    Result: Completed. Each card now shows Win : Loss with the win percentage, using the SAME perf>0/perf<0 split as the detail panel the card opens into, so the two cannot disagree. Deliberately not the existing _wl() helper, which ranks candidates for the "Best win : loss" card using a +/-0.5% break-even dead-band; that is a selection heuristic, this is a displayed count, and the distinction is recorded at both sites.
    Status: complete

49. Time: 14:11:00
    Request: P-06 "Apply this configuration" button - align at the bottom of the card and centre it horizontally.
    Result: Completed. The button is pinned to the bottom of its card and centred. The cards are equal-height flex items with variable-length copy above, so previously each button floated wherever its own card's text happened to end; the body now stretches and the button sits in an auto-margin footer.
    Status: complete

50. Time: 14:12:00
    Request: P-01 BUG After clicking "Apply this configuration" the page can warn that it is slow, and afterwards it is not clear whether the settings saved.
    Result: Completed. Applying triggers several heavy re-renders that can stall the tab for longer than the button's four-second confirmation lasted, so by the time the page responded the only evidence it had worked was already gone. A persistent banner above the cards now records the outcome with the time it saved, states plainly when it did NOT save and that the configuration is unchanged, and is announced to screen readers.
    Status: complete

51. Time: 14:13:00
    Request: P-01 BUG Transaction evidence must show a "Data loading" message while it is loading.
    Result: Completed. The transaction-evidence loading state now leads with the house "Data loading" wording, keeping the reassurance from item 12 -- which configuration is being replayed, and that it does not fetch data, change settings or place orders -- in the line beneath.
    Status: complete

52. Time: 14:14:00
    Request: P-01 BUG "Capital efficient" card selected but the evidence table is empty.
    Result: Completed. Root cause found by reproducing it live rather than reading the code: "Capital efficient" had 64 eligible trades, 45 funded and a 42.4% return, but a STORED proof of zero rows, while a fresh replay of the same inputs returned all 64. An empty array is truthy, so a proof calculated before seq was populated was cached permanently and the guard `if(!x.proof)` never retried it. The evidence table therefore rendered with no rows. It now retries when a stored proof disagrees with the population it came from, bounded to three attempts; a genuinely empty population still falls through to the table's own empty-state message.
    Status: complete

53. Time: 15:05:00
    Request: P-01 BUG A "Data loading" message must not be the last thing shown when something has actually failed; and some forms display literal template text such as 'Data loading - " " variable'.
    Result: Completed. The visible debris was self-inflicted: normalising the loading wording earlier on 2026-08-23 used a pattern that stopped at the first quote, which stripped the opening of a template expression and left `" "+label:""}` rendering literally on screen. Repaired, and a test now fails on any template fragment following a loading message. Separately, every admin loading row can now become an explicit fault: the same row is replaced with what went wrong and a retry control, driven both by the fetch's catch handler and by a watchdog that fires if the request simply HANGS rather than rejecting, which no catch handler can cover and which would otherwise spin forever and merely look like slow data.
    Status: complete

54. Time: 16:20:00
    Request: P-10 The same wallet-replay maths is implemented in both Python and JavaScript with nothing asserting the two agree; add a cross-language equivalence check.
    Result: Completed, and it found a real divergence on its first run. hvf_web/server.py::_sqa_compound and hvf_web/index.html::_combReplay both implement position sizing as a percentage of running equity, the max-open cap, margin reserved until exit and the broker-minimum skip -- the server docstring says outright that it uses "the same contract as the browser wallet", and the leverage table is a duplicated literal in both. Both were already well tested, but SEPARATELY: four tests exercised the Python replay and several ran the JavaScript one in Node, while no test fed identical input to both. For money maths that is the gap that matters, as shown earlier the same day when a one-line reordering inside _combReplay moved the reported wallet by GBP 1,037 with the suite still green. test_replay_equivalence.py now runs both over one shared population across five position-size/max-open models and compares funded count, skipped count and ending wallet to the pound, with the five contract differences a caller must normalise pinned explicitly (pounds versus fractions, absolute versus wallet-relative minimum trade, who filters the population, return_pct versus perf, and the funded cap). It includes a test that deliberately feeds mismatched models and asserts the comparison fails, so the detector cannot be vacuous.
    Status: complete

55. Time: 16:25:00
    Request: P-01 BUG The server-side replay capped concurrent positions one below the browser for most position sizes.
    Result: Completed. `1.0 // 0.05` evaluates to 19.0, not 20, because 1/0.05 floats to 19.999999999999996, so the Python replay allowed one fewer concurrent position than the browser's Math.floor(1/0.05). Systematic rather than a single case: 2% gave 49 against 50, 4% gave 24 against 25, 5% gave 19 against 20, 10% gave 9 against 10 and 20% gave 4 against 5, agreeing only at 25% and 50%. It also contradicted the function's own docstring, which states that a 4% model cannot hold more than 25 positions while the code computed 24. The browser was correct. Measured on the real 11,682-row three-year population before shipping: on the shipped 5%/20 model the server Back Test moves from GBP 12,692 to GBP 12,828 and funds 442 trades rather than 419; 4%/25 moves +GBP 213, 20%/5 +GBP 448, and 10%/12 moves DOWN GBP 145 because funding more positions also takes more losers; 2%/25 is unchanged. Found by item 54 on its first execution.
    Status: complete

56. Time: 17:10:00
    Request: P-06 Check each tab's performance and confirm a "Data loading" message is implemented wherever one is needed.
    Result: Completed. Audited all 29 tabs against the showTab dispatch map, tracing each renderer and anything it delegates to. Four fetched with no loading state at all and have been fixed: Documents, Order Operations, User Management and X Posts, each of which previously showed an empty table or a bare panel while its request was in flight -- during a slow admin query that is indistinguishable from "there is nothing here". Documents also used its own "Loading" wording, as did the single-guide opener; both now use the house message. All four report an explicit fault with a retry if the request fails or hangs, using the shared helpers. A parametrised test now walks the dispatch map and fails if any fetching tab lacks a loading state, plus a companion test that fails when a tab is added to the map without being added to the audit, so a new tab cannot quietly escape the check. On performance: the earlier measurements stand -- the two costs worth fixing were the evidence-table render and the winners endpoints, both addressed; the remaining client cost is the Scanner's roughly 500 ms render over 1,773 records, which is noticeable on filtering but not a freeze.
    Status: complete

57. Time: 09:30:00
    Request: P-20 Extract the client JavaScript out of index.html so edits stop being pattern-matching on a 616 KB single file.
    Result: Completed, deployed and live-verified. index.html held 1,953 lines of markup followed by ONE script block of 4,613 lines, and every behaviour change meant editing inside that block by text pattern rather than by opening a function -- which is how a template expression got cut in half on 2026-08-23 and shipped rendering literally on screen. The script now lives in hvf_web/app.js, byte-identical (verified by hash); index.html drops from 617 KB to 200 KB and loads it with a relative tag. Deliberately ONE file rather than several modules: the 249 functions share a single global scope with no module system, so splitting them would not create real boundaries, only load-order risk. Three deployment concerns handled: the packager ships app.js to BOTH serving paths, because Apache serves the domain root copy and never reaches Flask while Flask serves hvf_web/, and a page whose script 404s renders and then silently does nothing; a Flask route serves it for local and non-Apache hosts; and both Apache and that route revalidate it, because a browser holding yesterday's script against today's markup is a silent failure. Tests: a shared client_source() helper returns markup and script together so the roughly 55 existing assertions keep meaning what they meant, and four new tests pin that exactly one .js ships, that both copies are identical, that Flask serves it with no-store, and that Apache revalidates it. 426 tests pass. Verified on the live site after deployment: /app.js returns 200 as text/javascript with cache-control no-cache, must-revalidate; the served page carries exactly one script tag pointing at it and no inline block; the served script contains every function checked. In the browser the script EXECUTED -- six named functions defined, 20 tab buttons rendered, no console errors -- and the Scanner populated all 1,773 instruments after its /api/records call, which itself took 14.4 s and correctly showed the Data loading row throughout.
    Status: complete

58. Time: 10:15:00
    Request: P-10 Make the Let Winners Run decision a short decision rather than a working session.
    Result: Completed. Added LWR_OBSERVE_ONLY, an observe-only mode that runs the complete let-winners-run decision path against the real account -- same owner-scoped sessions, same live quotes, same target and handover arithmetic -- and records what it WOULD have done without calling a single mutating IG endpoint. It is independent of the live switch so the two cannot be confused, and both ship off. Six tests assert it stays silent even when a position qualifies for action, which is the only reason to trust it. Also wrote docs/LET_WINNERS_RUN_DECISION.md: what the feature does, the measured historical evidence, the six guards already enforced, what remains unverified, and the risk that matters most -- removing the IG take-profit makes the gain depend on the manager continuing to run, while the Order Bridge runs only every two hours on weekdays, leaving positions unmanaged overnight and at weekends with nothing detecting it. The brief ends with three tick-box decisions and a recommendation to observe first.
    Status: complete

59. Time: 11:20:00
    Request: P-10 Add the watchdog that must exist before Let Winners Run could ever go live.
    Result: Completed. Let Winners Run places orders with NO take-profit, so a gain is protected only while the stop manager keeps running; with a take-profit the broker holds it even if every system of ours is down. run_let_winners_run() now writes a heartbeat on each completed pass recording when it ran, in which mode, and how many open positions were bound to it. run_lwr_watchdog.py reads only that heartbeat and alerts to Slack #alerts when positions were relying on the manager and it has not run within the threshold. Deliberately its own scheduled job rather than part of the Order Bridge, because a watchdog inside the thing it watches is useless in the one case that matters; it holds no IG credentials and a test asserts it cannot reach any mutating call. Quiet by design: silent when the manager has never run (the feature is off and positions keep their take-profit) and when the last pass had nothing bound to it, because a watchdog that cries wolf gets muted. Registered hourly on weekdays, covering the two-hourly bridge. Nine tests including the normal-gap, weekend-gap and channel-switch cases. The heartbeat write can never raise into a trading pass.
    Status: complete
