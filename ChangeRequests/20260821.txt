HVF Web Application — Change Requests

## Testing and quality

1. Time: 09:00:00
   Request: Reconcile every Back Test summary figure with its funded transaction-evidence rows.
   Result: In progress. A replay-level reconciliation gate and regression coverage are being added; a mismatch will withhold the headline result.
   Status: in progress

2. Time: 09:30:00
   Request: Set the Replay model defaults to a 5% maximum position size and 20 maximum open positions.
   Result: Completed and publicly verified: Replay defaults are 5% and 20. Saved user configuration is unchanged.
   Status: complete

3. Time: 10:15:00
   Request: Following a full review, make Let winners run simulation-only until per-user account routing and a safe live handover are implemented and verified.
   Result: In progress. The live path is fail-closed; the historical report remains available. The legacy Owner trading-profile UUID is explicitly mapped to Alex without rewriting stored positions, and the two-hour bridge now calls the guarded manager even when no new candidate is found.
   Status: in progress

4. Time: 13:45:00
   Request: Restore meaningful GitHub Offline Python Regression Test status after stale checks caused every recent workflow run to fail.
   Result: Completed and verified by GitHub Actions run 32483421114: the full offline suite passed after deployment-only IONOS secrets were excluded and the approved 20-position default was reflected in the regression test.
   Status: complete

5. Time: 14:10:00
   Request: Replace the Best over 3 years card's finalist shortlist with a full, auditable configuration-grid review and repeat the validation daily.
   Result: In progress. The card now replays every generated, enforceable three-year configuration rather than reducing candidates to 40 and then 12. A server-side daily persisted audit is still required so this full-grid result is revalidated after data refresh without depending on a user opening the browser page.
   Status: in progress

6. Time: 14:25:00
   Request: Make Best Settings transaction evidence reconcile visibly with its return card by adding a Net gain total at the bottom of the table.
   Result: In progress. The total will be calculated from the same funded proof rows as the card; a cross-filter will show a labelled visible subtotal rather than an incorrectly implied full-card total.
   Status: in progress

7. Time: 14:30:00
   Request: Remove the unused Closed date/time column from the IG Account transaction table; closed transactions are hidden by default.
   Result: Withdrawn by the requester before release. The Closed date/time column remains.
   Status: cancelled

8. Time: 14:35:00
   Request: Populate the required Now column when IG Account uses Aggregate rows.
   Result: In progress. Aggregate rows will show the size-weighted current price from their open-position components instead of an artificial blank.
   Status: in progress

9. Time: 14:40:00
   Request: Allow a user to select their own open IG positions and close them only after an explicit confirmation.
   Result: In progress. The endpoint must re-read the acting user's open positions, accept only their selected deal IDs, and require a confirmation flag before sequential market-close requests are sent.
   Status: in progress
